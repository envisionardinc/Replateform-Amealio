# Migration Status

Single source of truth for the controlled replatforming. Update this file whenever a phase, activity, blocker, decision, or migrated capability changes.

- **Last updated:** 2026-09-03 (P1.7.37 — Tip/Donation Lifecycle & Economic Contract — FORENSIC ONLY; traced `Order.tipMinor`/`donationMinor` through creation/capture/lifecycle/refund/settlement/payout; **critical finding: tip/donation are customer *intent only* — never collected, held, disbursed, or refundable** because the money chain is anchored on `grandTotalMinor = PaymentIntent.amountMinor` and P1.7.36 keeps tip/donation outside it; `Settlement.payoutType` hardcoded `'ORDER'`, `ORDER_TIP` enum never produced, no beneficiary/charity code; **Go/No-Go: NO-GO** for payout routing/charity/refund next, **GO** for P1.7.38 collection-capture foundation (owner-decision gated); no code/schema change; 401/401 unchanged)
- **Prev:** 2026-09-03 (P1.7.36 — Tip & Donation Order-Model Foundation — data-model foundation; one additive migration `20260903050000_p1_7_36_order_tip_donation`; adds `Order.tipMinor`/`Order.donationMinor` (BigInt NOT NULL default 0) held OUTSIDE grand-total and the commission basis; **settlement economics UNCHANGED**; refund semantics DEFERRED; all 1681 existing rows backfilled to 0, `order_total_integrity` CHECK unchanged; 391/391 → **401/401**)
- **Prev:** 2026-09-03 (P1.7.35 — Payout Charges & Financial Deduction Reconciliation — FORENSIC ONLY; reconciled the full legacy payout-pool flow; every additional deduction deferred/blocked — tips/donations not modeled in target Order (DR-TIP/DR-DONATION), customer-GST-remittance + outgoing-charge GST BLOCKED (DR-03a), outgoing/gateway charge rate config-not-in-target (DR-PAYOUT-CHARGE), ADMIN reimbursement has unresolved full-refund clawback (DR-ADMIN-DISCOUNT-REIMBURSEMENT DEFERRED); current target model confirmed correct/minimal; NO code/schema change; 391/391 unchanged)
- **Current phase:** Phase 1 — Target Repository Baseline & Governance
- **Overall state:** Documentation & governance only. No application code, schema, or scaffolded apps yet.

## Phase model

| Phase | Description | State |
|-------|-------------|-------|
| 0 | Discovery & architecture design | ✅ Completed (in review) |
| 1 | Target repository baseline & governance | 🔵 **Current** |
| 2 | Restore India baseline (from designated source) | ⏳ Next |
| 3 | Baseline validation | ⏳ Pending |
| 4 | Progressive capability introduction | ⏳ Pending |

## Completed activities

- **Phase 0 — Discovery** (in review): forensic, read-only inventory of the India platform → `docs/migration/01–10`.
  - Reference inventory, 17-domain inventory, API surface (~419 Feathers mounts + Nest API), MongoDB usage & business entities/relationships, business rules, integrations, auth/authorization, workflows, design-system inventory, risks + recommended order.
- **Phase 0 — Target design** (in review): `docs/architecture/` — canonical PostgreSQL/Prisma domain model, ERD, multi-tenancy, India-first localization, and the approved target repository structure.
- **Phase 1 — Governance:** established engineering rules (`AGENTS.md`), project overview (`README.md`), migration hub (`docs/migration/README.md`), this status tracker, and the decision log (`DECISIONS.md`).
- **Phase 1 — P0.2 Repository landscape:** inventoried and classified all 6 available repositories (30-point each) and produced the India-baseline comparison → [REPOSITORY_LANDSCAPE.md](./REPOSITORY_LANDSCAPE.md) and [SOURCE_REPOSITORIES.md](./SOURCE_REPOSITORIES.md).
- **Phase 1 — P0.3 Baseline source decision (APPROVED):** owner-approved baseline source set → [india-baseline/BASELINE_SOURCE_DECISION.md](./india-baseline/BASELINE_SOURCE_DECISION.md) (D-011). Core = `amealio-vendordashboard` + `amealio_web_app` + `amealiodashboardmvp-`; deferred = `amealio-nestjs-backend`, `amealio-self-delivery-app`. **Blocker B1 resolved.**
- **P1.1 — Deep India Baseline System Analysis: COMPLETE.** Analyzed the three approved repos as one logical platform across all 13 required areas → [india-baseline/](./india-baseline/) (`01`–`13`). Analysis only.
- **P1.2 — Define the India Baseline Capability Matrix: COMPLETE.** Capability matrix + acceptance criteria + Definition of Done + critical journeys → [14](./india-baseline/14-CAPABILITY-MATRIX.md)–[16](./india-baseline/16-END-TO-END-BASELINE-JOURNEYS.md).
- **P1.3 — Create the Target Migration Map: COMPLETE.** Defined *how* the approved baseline is replatformed (design/mapping only) → [india-baseline/17-TARGET-MIGRATION-MAP.md](./india-baseline/17-TARGET-MIGRATION-MAP.md) + [target-architecture/](./target-architecture/) (`01`–`12`): architecture options (recommend modular monolith + service seams), domain boundaries, source→target mapping (REUSE/ADAPT/REFACTOR/REIMPLEMENT/REPLACE/DEPRECATE), conceptual data map (no schema), API/frontend/auth/integration/realtime maps, domain-by-domain sequence, carried-forward owner decisions, and P0/P1/P2 risks. No code, schema, data migration, or production changes; deferred repos not introduced; owner-decisions not resolved.

- **P1.4 — Design the Target PostgreSQL Data Model: COMPLETE.** Documented the conceptual target data model → [database/](./database/) (`01`–`18`): domain model, entity↔legacy mapping, identifier strategy, relationships/constraints, enum/status strategy (OD-11), money/pricing, order model, payment/settlement model, reservation model, notification model, audit/soft-delete, ownership, indexing, migration complexity, integrity rules, extension seams, ERD, and the decision register. **Design only — no schema, migrations, tables, data migration, or code.** Owner-decisions and OD-11 enum mappings are **not** resolved.
  - **Blocked data-model decisions (require owner/data input):** DR-02a..e (OD-11 numeric enum mappings — order/payment status, payment method, `t_type`, wallet role) — **gate Orders/Payments/Wallet data migration**; DR-03a (India GST components/rates); DR-14/15/16/17 (Celebrations/Events/Ticketing, ONDC, Loyalty, Wallet inclusion — owner-decision); DR-07a (enterprise Organization tenancy). See [database/18-DATA-MODEL-DECISIONS.md](./database/18-DATA-MODEL-DECISIONS.md).

- **P1.5 — Build the PostgreSQL Development Foundation: COMPLETE.** First target-database implementation task. Established a reproducible local PostgreSQL 16 dev environment (Docker `docker-compose.yml` + local fallback), Prisma 5 (D-004) tooling, `.env.example` (dev/test only), and **Prisma migrations implementing the approved P1.4 baseline model** (CORE + OPTIONAL entities; deferred/owner-decision domains excluded). Added synthetic seed data and an automated validation suite (**11/11 passing**). Docs: [database/19-DEVELOPMENT-DATABASE.md](./database/19-DEVELOPMENT-DATABASE.md), [database/20-SCHEMA-IMPLEMENTATION.md](./database/20-SCHEMA-IMPLEMENTATION.md). **No production access, no MongoDB changes, no legacy data migrated; OD-11 and GST rates not guessed.**

- **P1.6 — Build the NestJS Application Foundation: COMPLETE.** Second implementation task. Established the `apps/api` NestJS 10 modular-monolith foundation on the P1.5 database (Prisma integration via a global `PrismaService`, typed/validated config, `/api/v1/health`, structured logging + request-id, global exception filter with a consistent error shape, `ValidationPipe`, URI versioning, CORS, graceful shutdown), plus **conventions** for module boundaries, domain events (in-process port), provider/integration ports, and security decorators (metadata only). Single test runner (Jest + ts-jest). **No business domains, integrations, or authentication implemented.** Docs: [application/21-NESTJS-FOUNDATION.md](./application/21-NESTJS-FOUNDATION.md).
  - Validation: `npm run build` ✓, `npm run lint` ✓, `npm run format:check` ✓, `npm test` → **19/19 passing** (config validation, DB schema 11/11, app bootstrap + health + error-shape), local `node dist/main.js` startup + `/api/v1/health` = `{status:ok, db:up}` ✓. P1.5 migrations/schema unchanged and intact.

- **P1.7.1 — Identity (analyze + minimal foundation): COMPLETE.** First business-domain migration. Read-only analysis of baseline auth across all three repos (file-cited; consumer vs merchant vs admin differences documented) → [domains/22-IDENTITY-ANALYSIS.md](./domains/22-IDENTITY-ANALYSIS.md). Implemented a minimal, evidence-backed Identity foundation in `apps/api/src/modules/identity/`: consumer user management (register/get; duplicate→409; unverified default), `PasswordHasher` port + bcrypt adapter (baseline algorithm), `UserRepository` port + Prisma adapter, and role-based authorization infra (`RolesGuard` + `Principal` + `CurrentUser`). **No schema change** (P1.5 intact); **no token/OTP/social auth, no permission-tree evaluator, no HTTP endpoints, no data migration.** Validation: build ✓, lint ✓, format ✓, **54/54 tests passing** (32 Identity + app/config/DB). OD-11 untouched.

- **P1.6.1 — TurboRepo Monorepo Foundation: COMPLETE.** Introduced Turbo (`turbo@2.10.12`) + npm workspaces as the orchestration layer, promoting the existing NestJS API to the `@amealio/api` workspace (`apps/api`). **Prisma kept at root** (no move); P1.5 schema/migrations **unchanged**. Root commands delegate to Turbo (`build`/`test`/`lint`) with a repo-wide `format:check`; test task is uncached (DB-dependent). Docs: [architecture/23-TURBOREPO-MONOREPO.md](./architecture/23-TURBOREPO-MONOREPO.md). Validation: build ✓, lint ✓, format:check ✓, **54/54 tests**, API starts + `/api/v1/health` ok, `db:validate` 11/11, `migrate status` up to date — no regression to P1.5/P1.6/P1.7.1.

- **P1.7.1A — Authentication Architecture & Migration Decision: COMPLETE.** Decision/architecture only (no auth implemented). Re-confirmed consumer/merchant/admin auth from source; produced the authentication behavior matrix, legacy-quirk classification, and the recommended target (**Option C — unified canonical Identity with distinct principals**: consumer `User` + staff `StaffMember`), plus target token/session + authorization design, the **merchant/admin boundary**, future user-migration + cutover strategies, PostgreSQL impact, and an owner-decision register. Docs: [domains/24-AUTHENTICATION-ARCHITECTURE.md](./domains/24-AUTHENTICATION-ARCHITECTURE.md). **OD-11 confirmed irrelevant to auth.** Consumer auth is schema-sufficient; **staff/admin auth requires future schema additions (AUTH-D8, reviewed migration)**. Validation: build/lint/format ✓, 54/54 tests, Prisma schema/migrations unchanged.

- **P1.7.1B — Consumer Authentication: COMPLETE** (local/dev only; no cutover). Implemented consumer password authentication on the target platform (`apps/api/src/modules/identity/authentication/`): register/login/refresh/logout/me, **Bearer access JWT** + **rotating server-side refresh sessions** (sha256-hashed, revocable, **replay-detected**), a **JWT consumer guard**, and blocked-status enforcement — reusing the P1.5 `User`+`Session` (**no schema change**). Feature-flagged (`CONSUMER_AUTH_ENABLED`); legacy raw-header rejected; legacy apps untouched. Deferred: OTP/phone-only/social/WhatsApp/reset/verification and staff/admin/merchant auth. Docs: [domains/25-CONSUMER-AUTHENTICATION.md](./domains/25-CONSUMER-AUTHENTICATION.md). Validation: build/lint/format ✓, **75/75 tests** (54 prior + 21 new), live smoke ✓ (register→login→me→refresh-rotate→replay-401→logout→refresh-401), Prisma schema/migrations unchanged.

- **P1.7.1C — Staff/Admin Authentication & Identity Schema Design: COMPLETE** (design/analysis only — **nothing implemented**). Resolved **AUTH-D8** as an implementation-ready design and settled the remaining staff/admin identity questions. Re-inspected the legacy `VendorUser`/`role-management`/`admin-auth` (bcrypt; admin logs in via `userId`; admin token minted with the **consumer** `authentication` secret — a defect to drop; stateless JWT, no refresh rotation; deep boolean permission trees; act-as exists) and the target models. **Recommended target:** SUPER_ADMIN = `StaffMember` with **nullable `merchantId`**; **separate `StaffCredential`** (one `PASSWORD` row now; extensible) and **separate `StaffSession`** (rotating hashed refresh — leaves consumer `Session`/P1.7.1B untouched); `status StaffAccountStatus{ACTIVE,BLOCKED}` + `deletedAt`; RBAC on the **existing** `Role`/`RolePermission`; act-as-merchant deferred but modelled as explicit/audited (`StaffSession.actAsMerchantId` + existing `AuditLog`). Future Prisma delta (NOT applied): +2 models, +2 enums, 3 `StaffMember` modifications — all additive/reversible. Docs: [domains/26-STAFF-ADMIN-AUTHENTICATION-DESIGN.md](./domains/26-STAFF-ADMIN-AUTHENTICATION-DESIGN.md). **AUTH-D8 design-complete pending owner schema sign-off; AUTH-D4/D5/D6/D7/D9 remain owner decisions (recommendations recorded).** Validation: build/lint/format ✓, 75/75 tests, **`prisma/schema.prisma` + migrations unchanged**, no application/frontend/data changes.

- **P1.7.1D — Staff/Admin Identity PostgreSQL Schema: COMPLETE** (schema only — **no authentication implemented**). Implemented the AUTH-D8 schema foundation from doc 26 in one additive migration `20260902010630_p1_7_1d_staff_admin_identity`: **modified `StaffMember`** (`merchantId` → nullable so NULL = SUPER_ADMIN; added `status StaffAccountStatus{ACTIVE,BLOCKED}`, `legacyId @unique`, `@@index([status])`; FK → `Merchant` set to `onDelete: Restrict`), **added `StaffCredential`** (`secretHash`, `type StaffCredentialType{PASSWORD}`, unique `(staffMemberId,type)`) and **added `StaffSession`** (hashed `refreshTokenHash @unique`, `expiresAt`, indexes) — leaving the consumer `Session`/P1.7.1B **untouched**; reused existing `Role`/`RolePermission`. Login-identifier (email/phone) uniqueness intentionally **not** constrained (**O1 open**); act-as `actAsMerchantId` **not** added (deferred). No staff/admin login/JWT/refresh/logout/guards/RBAC/act-as, no data migration, no MongoDB/frontend/source changes. Docs: [domains/26-STAFF-ADMIN-AUTHENTICATION-DESIGN.md](./domains/26-STAFF-ADMIN-AUTHENTICATION-DESIGN.md) §24. Validation: build/lint/format ✓, **85/85 tests** (75 prior + 10 new schema tests), `prisma validate` ✓, `migrate status` up to date (dev + test), P1.5 migrations unmodified.

- **P1.7.1E — Staff/Admin Authentication: COMPLETE** (local/dev only; no cutover). Implemented password-based staff/admin authentication in `apps/api/src/modules/identity/staff-authentication/`: login/refresh/logout/`me`, **Bearer access JWT** (dedicated `STAFF_JWT_ACCESS_SECRET` + `aud=amealio-staff`, minimal claims incl. `actorType`/`staffRole`/server-derived `mid`) and **rotating server-side `StaffSession` refresh** (sha256-hashed, constant-time compare, **replay-detected + revocable**), a **`JwtStaffGuard`** that re-checks status per request, and **blocked (403)/deleted (401)** enforcement at login/refresh/guard. SUPER_ADMIN recognized as `StaffMember` with `merchantId=NULL`; merchant scope server-derived (never from request input). **Mandatory preflight:** `StaffSession` supports rotation/revocation/replay/logout via row-delete — **no schema change** (Prisma untouched). Feature-flagged (`STAFF_AUTH_ENABLED`); legacy raw-header/consumer-secret/stateless behavior rejected; consumer auth (P1.7.1B) untouched. **No** RBAC/permission enforcement, act-as, OTP/social/reset, or VendorUser/admin **data migration**. Docs: [domains/27-STAFF-ADMIN-AUTHENTICATION.md](./domains/27-STAFF-ADMIN-AUTHENTICATION.md). Validation: build/lint/format ✓, **119/119 tests** (85 prior + 34 new), live smoke ✓ (login→me→refresh-rotate→replay-401→logout→refresh-401, cross-guard 401), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**.

- **P1.7.1F — Staff/Admin RBAC / Authorization Foundation: COMPLETE** (local/dev; foundation only). Implemented the reusable staff/admin **authorization** layer in `apps/api/src/modules/identity/staff-authentication/authorization/`: a **`StaffAuthorizationGuard`** that composes after `JwtStaffGuard`, plus decorators **`@RequireStaffRoles`** (ANY), **`@RequireStaffPermissions`** (ALL, enforced against the existing `Role`/`RolePermission` via `StaffPermissionRepository`, deny-by-default), **`@PlatformOnly`** (SUPER_ADMIN), and **`@MerchantScoped`** (merchant staff confined to server-derived `StaffPrincipal.merchantId`; request-supplied merchant id rejected, never trusted). **401** (no principal) vs **403** (authenticated-but-unauthorized); SUPER_ADMIN = platform superuser (bypasses gates, not merchant-restricted). **No token-claim change** (permissions read per-request from DB), **no Prisma change**, **no act-as**, **no legacy permission-catalogue migration** (AUTH-D6 pending), **no frontend/domain authorization**. Docs: [domains/28-STAFF-ADMIN-AUTHORIZATION.md](./domains/28-STAFF-ADMIN-AUTHORIZATION.md). Validation: build/lint/format ✓, **139/139 tests** (119 prior + 20 new: 12 unit + 8 e2e), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**; P1.7.1E tests unchanged and green.

- **P1.7.2 — Merchant & Location Foundation: COMPLETE** (application module only; **no schema change**). Grounded the existing P1.5 tenant/location tables in the current-state source: legacy has **no `Merchant` entity** (owner = `VendorUser` via `restaurant.vendor_id`; business unit = `restaurant`; `subscription` keyed by `vendor_id` embeds `table_setup`), so the target `Merchant` is the approved P1.4 tenant abstraction grouping a vendor's restaurants (**Merchant 1 → N Restaurant**, `Restaurant.merchantId` NOT NULL). Added a minimal `apps/api/src/modules/merchant/` module — `MerchantRepository` + `RestaurantRepository` (read-only lookups incl. `legacyId`, `listByMerchant`, `belongsToMerchant`) + **`MerchantScopeService`** (data-aware tenancy: confirms a target restaurant belongs to the staff's server-derived `StaffPrincipal.merchantId`; SUPER_ADMIN platform-scoped). **No CRUD/controllers/frontend**, **no Subscription redesign** (kept `config Json?` placeholder — not flattened), **no auth change**. Legacy→Merchant import grouping + subscription-config modeling recorded as **UNKNOWN/deferred**. Docs: [domains/29-MERCHANT-LOCATION-FOUNDATION.md](./domains/29-MERCHANT-LOCATION-FOUNDATION.md). Validation: build/lint/format ✓, **151/151 tests** (139 prior + 12 new), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**; P1.7.1E/F suites green.

- **P1.7.3 — Merchant Subscription & Configuration Foundation: COMPLETE** (application module only; **no schema change**). Traced the legacy `subscription.model.ts` (2319 lines, keyed by `vendor_id`, numeric `status` — **not billing**; top-level business-type entitlement booleans; per-type `*_status` blocks embedding `seating`/`walkin_waitlist`/`reservation`/`table_management.table_setup`/`event_management`/`experience_management`/`ordering`). Reused the existing `Subscription{merchantId, restaurantId?, productType, status, config Json?}` — **`config Json?` preserved (not flattened)**. Added `apps/api/src/modules/subscription/`: `SubscriptionRepository` (read-only; by-merchant/active/exists; missing-safe), `SubscriptionConfigService` (safe, **unknown-preserving** accessor + CONFIRMED business-type entitlement reads + raw `getTableSetup` locator — shape NOT asserted), `SubscriptionService` (P1.7.1F merchant tenancy: server-derived scope, cross-merchant 403, SUPER_ADMIN explicit-target only). Classified config as CONFIRMED/PARTIAL/UNKNOWN/DEAD; **`table_setup` preserved + documented, NOT normalized** (seating-phase deferral). No billing/CRUD/controllers/frontend; no feature-flag platform; no auth change. Docs: [domains/30-SUBSCRIPTION-CONFIGURATION-FOUNDATION.md](./domains/30-SUBSCRIPTION-CONFIGURATION-FOUNDATION.md). Validation: build/lint/format ✓, **166/166 tests** (151 prior + 15 new), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**; P1.7.1E/F suites green.

- **P1.7.4 — Platform Foundational Data (taxonomy): COMPLETE** (reconciliation + minimal read foundation; small **additive** schema change). Established (from the shared backend `amealio-vendordashboard`) that platform taxonomy is **admin-defined → merchant-selected → user-consumed**: legacy `Category` (parent) + **`Sub Category`** (child, `createdBy:"admin"`, referenced by `restaurant.selected_*` for cuisine/mood/dress-code/parking/etc.) = a **unified hierarchy**; `Cusine` is a dedicated lookup; **icons/media are EMBEDDED string fields** (icon/iconCode/hexColor/photo) — **not** entities. Target already models this as `Category` (hierarchical) + `Cuisine`; **extended** both with `legacyId @unique` + embedded icon/media + `status` (+`Category @@index([type])`) — **no new tables**, no Icon/Mood/MasterData normalization. Added read-only `apps/api/src/modules/reference-data/` (`CategoryRepository` + `CuisineRepository`). `menuCategory` = `MenuSection` (merchant menu, not platform taxonomy — no conflation). **Deferred/UNKNOWN:** Mood/MoodManagement/Craving discovery overlays, cuisine dedup (`Cusine` vs Sub Category), type/status vocabulary, attribute→Category-vs-RestaurantFeature mapping, Experience/Event/Festival taxonomy — all owner decisions. Migration `20260902035959_p1_7_4_platform_foundational_data` (dev+test). Docs: [domains/31-PLATFORM-FOUNDATIONAL-DATA.md](./domains/31-PLATFORM-FOUNDATIONAL-DATA.md) (incl. owner/source matrix). Validation: build/lint/format ✓, **172/172 tests** (166 prior + 6 new), `prisma validate` ✓, `migrate status` up to date, historical migrations unmodified; P1.7.1E/F/2/3 suites green.

- **P1.7.5 — Menu & Catalog Read Foundation: COMPLETE** (application module only; **no schema change** — reconciliation confirmed the catalog schema was already complete). Established the canonical **read** foundation for the merchant-owned catalog (legacy `menu`/`menuCategory`/`vendorItems` → target `Menu → MenuSection(→optional Category) → MenuItem → ItemVariant` + `ItemChannelConfig` + `AddOnGroup/AddOn`). Added `apps/api/src/modules/catalog/`: `MenuRepository` + `MenuItemRepository` (read-only: by id/legacyId, list by restaurant/merchant/menu/section, item detail with variants/channels/add-ons, **availability filtering** + **soft-delete** exclusion, missing-ref safe) + **`CatalogService`** (merchant tenancy via P1.7.2 `MerchantScopeService.assertRestaurantInScope` over the server-derived `StaffPrincipal`; cross-merchant 403; SUPER_ADMIN platform-scoped). **Exact BigInt minor-unit money** preserved. `MenuSection.categoryId → Category` kept optional (no duplication; `menuCategory`=`MenuSection`, not platform taxonomy). No menu CRUD/controllers/ordering/cart/POS/discovery/frontend. Docs: [domains/33-MENU-CATALOG-FOUNDATION.md](./domains/33-MENU-CATALOG-FOUNDATION.md). Validation: build/lint/format ✓, **182/182 tests** (172 prior + 10 new), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**; P1.7.1E/F/2/3/4 suites green.

- **P1.7.6A — Super Admin platform foundation reconciliation (checkpoint, discovery only):** audited the full Super Admin reference layer against source. Findings: legacy has a real **`currency`** reference table (ISO/symbol/status) and a **`media-catalogue`** reusable image repository — both **MISSING** in target (currency = embedded `currencyCode` + BigInt minor units; icons = embedded strings); geography is a flat `{text}` `countryStateCity` lookup + embedded strings (no normalized hierarchy either side). **Proven negatives:** no platform **Item Catalog** (`catalogue`/`chaincatalogue` group *merchant* `vendorItems`) and no platform **Experience Catalog** (`Experience` is merchant-owned `vendorId`; platform layer = `exp-events` taxonomy) — so `MenuItem` (P1.7.5) is the correct level and no `Celebration`/catalog entity should be created. **P1.7.4 = PARTIAL by design** (taxonomy done; currency/geo/media/discovery/experience-taxonomy remaining), but **nothing blocks the completed downstream for India-first** (embedded currencyCode/country + minor units suffice). **Recommendation (non-blocking): P1.7.6 = Currency (+ optional lightweight Geography) platform reference read foundation**; media-catalogue + discovery/experience taxonomy deferred to their domains. No hard-stop; no re-baselining. **No implementation** (no schema/module/migration). Docs: [domains/34-SUPERADMIN-PLATFORM-FOUNDATION-RECONCILIATION.md](./domains/34-SUPERADMIN-PLATFORM-FOUNDATION-RECONCILIATION.md) (inventory, ownership + dependency matrices, foundation graph, per-area findings, blockers, owner decisions, proposed boundary).

- **P1.7.6 — Currency Platform Reference Read Foundation: COMPLETE** (one small **additive** table). Added the missing canonical **`Currency`** platform reference (legacy `currency`; PLATFORM_DEFINED) — `isoCode @unique` (ISO 4217 canonical identity), `legacyId @unique`, `symbol`/`name`/`countryName`/`description`, `isActive` (mirrors legacy `status` bool), `deletedAt` (mirrors `is_deleted`). **Supplements** the embedded `currencyCode` (no money field migrated to a FK; **no monetary-representation change**; BigInt minor units unchanged). **No FX/conversion, no geography (no Country FK), no payments, no CRUD/controllers/frontend.** Added read-only `CurrencyRepository` to the existing `ReferenceDataModule` (findById/findByLegacyId/findByIsoCode/listActive/listAll; platform-global, no merchant tenancy). Migration `20260902050937_p1_7_6_currency_reference` (dev+test). Docs: [domains/35-CURRENCY-REFERENCE-FOUNDATION.md](./domains/35-CURRENCY-REFERENCE-FOUNDATION.md). Validation: build/lint/format ✓, **188/188 tests** (182 prior + 6 new), `prisma validate` ✓, `migrate status` up to date, historical migrations unmodified; P1.7.1E/F/2/3/4/5 suites green.

- **P1.7.7 — Merchant & User onboarding foundation reconciliation (checkpoint, discovery only):** traced merchant + user onboarding from source. **Conclusion: all platform foundations required before onboarding already exist** (Merchant/Restaurant P1.7.2, Subscription P1.7.3, Category/Cuisine P1.7.4, Currency P1.7.6; plus target `User`/`UserProfile`/`Address`/`ReferralProgram`). Onboarding **creates** VendorUser/restaurant/subscription + User/profile/address and **selects** existing platform taxonomy/currency; it persists **geography as strings** (`country/state/city` + `*_id` stored as String, not FKs; `countryStateCity` is flat `{text}`), **media as direct URLs**, and **currency as a string** — so normalized geography, media catalogue, and FX are **NOT onboarding prerequisites**; Mood/Craving are discovery-only. Genuinely missing = onboarding-state/profile-detail fields (part of the onboarding slice, additive). **India-first onboarding is SAFE** on the current foundations. **Recommendation: SAFE NEXT FOUNDATION IDENTIFIED — the Onboarding/User-Profile foundation** (reuse existing models; small additive onboarding-state/profile fields), with geography/media/discovery deferred. No hard blocker; no owner decision blocks starting. **No implementation** (no schema/module/migration). Docs: [domains/36-ONBOARDING-FOUNDATION-RECONCILIATION.md](./domains/36-ONBOARDING-FOUNDATION-RECONCILIATION.md) (prerequisite inventory, ownership/status tables, country-city/currency/media findings, dependency graph, evidence index).

- **P1.7.8 — Onboarding / User-Profile Foundation: COMPLETE** (bounded slice; small **additive** schema). Added onboarding/profile-state over existing models (no new entity): **`Merchant.onboardingSubmitted`** (← legacy `have_vendor_submitted_details`), **`Restaurant.onboardingStep`** (← `page_completed_till`, integer step) + **`Restaurant.softOnboarding`**, **`UserProfile.detailsSubmitted`** (← `have_submited_details_profile`) + **`UserProfile.completionPercentage`** (← `profile_percentage`); reused **`UserProfile.preferences Json`** for legacy preference arrays (no taxonomy invented). Added `apps/api/src/modules/onboarding/` (`MerchantOnboardingService` — merchant-tenant-scoped via P1.7.2 `MerchantScopeService`; cross-merchant 403; SUPER_ADMIN explicit-target) and `apps/api/src/modules/user-profile/` (`UserProfileService` — user-owned; preference **merge preserves unrelated keys**; `completionPercentage` 0..100 service invariant). No controllers/UI/workflow; **no geography/media/discovery/FX**; no change to auth/monetary. Migration `20260902053525_p1_7_8_onboarding_profile_state` (dev+test). Docs: [domains/37-ONBOARDING-USER-PROFILE-FOUNDATION.md](./domains/37-ONBOARDING-USER-PROFILE-FOUNDATION.md). Validation: build/lint/format ✓, **199/199 tests** (188 prior + 11 new), `prisma validate` ✓, `migrate status` up to date, historical migrations unmodified; P1.7.1E/F/2/3/4/5/6 suites green. **NO FRONTEND WAS MIGRATED.**

- **P1.7.9 — Onboarding workflow reconciliation (checkpoint, discovery only):** traced full merchant + user onboarding from source. **Key finding: onboarding progression/completion is FRONTEND-driven** — the dashboard PATCHes `page_completed_till` (integer step) as the merchant advances and sets `have_vendor_submitted_details` at submit (route guards read them); the user app FE-computes `profile_percentage` and sets `have_submited_details_profile` — none are backend-calculated. So there is **no backend onboarding "workflow engine"** to migrate. The real upstream gap is that all slices so far are **read foundations** (+ P1.7.8 state writes) with **no canonical create/write path** for Merchant/Restaurant/Subscription — which every merchant-owned downstream domain requires. Downstream remain **blocked**: Ordering (OD-11 numeric→named status), Seating (`table_setup` modeling), Discovery (Mood/Craving canonical). **Recommendation: SAFE NEXT SLICE = MERCHANT ONBOARDING WORKFLOW** (backend create/write foundation for Merchant+Restaurant+Subscription + onboarding-state transitions; FE wizard/percentage/validation, geography/media, and downstream deferred). No hard-stop; no owner decision blocks it. **No implementation** (schema/apps untouched; 199/199 unchanged). Docs: [domains/38-ONBOARDING-WORKFLOW-RECONCILIATION.md](./domains/38-ONBOARDING-WORKFLOW-RECONCILIATION.md) (step inventories, state semantics, foundation-vs-workflow gap, downstream/OD-11/table_setup/Mood-Craving reassessment, dependency graph, candidate ranking, evidence index).

- **P1.7.10 — Merchant/Restaurant/Subscription Creation (Write) Foundation: COMPLETE** (bounded upstream slice; **no schema change**). Implemented canonical creation over existing models: `createMerchant` (**SUPER_ADMIN/platform only** — new merchant has no tenant scope; legalName required; email/phone/legacyId unique), `createRestaurant` (**merchant-tenant-scoped**; name+merchantId; geography strings; onboarding defaults step=0/soft=false/status=ACTIVE), `createSubscription` (merchant-scoped; productType ∈ ORDERING|SEATING|EVENT|SCAN_PAY; status default ACTIVE; config Json; optional restaurant must be in scope). Server-resolved scope always wins over request `merchantId` (cross-merchant 403; unknown 404). **No forced atomicity** (legacy creates across steps; partial creation valid); duplicate protection = existing unique constraints (no idempotency framework). **Creation does not submit** — final submission stays the separate P1.7.8 `setMerchantSubmitted` transition (P1.7.9: FE-driven). Added `MerchantProvisioningService` + `MerchantProvisioningRepository` to the onboarding module (no controllers; read repos untouched). **No workflow engine, no frontend, no downstream domain, no billing, no geography/media normalization.** Docs: [domains/39-MERCHANT-CREATION-WRITE-FOUNDATION.md](./domains/39-MERCHANT-CREATION-WRITE-FOUNDATION.md). Validation: build/lint/format ✓, **210/210 tests** (199 prior + 11 new), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**; P1.7.1E/F/2/3/4/5/6/8 suites green. **NO FRONTEND WAS MIGRATED.**

- **P1.7.11 — Ordering lifecycle reconciliation (checkpoint, discovery only): OD-11 RESOLVED.** Traced native ordering from source: `ordering.model.ts` uses a **single numeric `order_status`** (`ORDERSTATUS` enum: INITIAL, PENDING, CONFIRMED, PREPARING, PACKING, READYTOPICK, ONTHEWAY, DELIVERED, COMPLETED, CANCELLED, RETURNED) whose **named members map 1:1** to the target `OrderStatus` (cosmetic renames READYTOPICK→READY, ONTHEWAY→ON_THE_WAY). The numeric **5/6** are just `ONTHEWAY`/`DELIVERED` encodings on that same field (rider advances it via `PATCH /orders/delivery-persons`); the named `DELIVERED`/`return_delivered` seen earlier were the **separate ONDC** lifecycle (deferred). `payment_status` is a **separate** numeric enum; the target already separates `OrderStatus`/`PaymentStatus`/`FulfillmentStatus`/`DeliveryTaskStatus` + `OrderStatusEvent` (← `previous_status`). **No separate order/delivery state machine, no schema change, no owner decision** blocks the order lifecycle. Peripheral maps (payment_status/method values, direct-merchant verification, POS/logistics sub-states, realtime) are deferred to their own slices. Ordering is the **best next candidate** (Seating blocked by table_setup; Discovery blocked by Mood/Craving). **No implementation** (schema/apps untouched; 210/210 unchanged). Docs: [domains/40-ORDERING-LIFECYCLE-RECONCILIATION.md](./domains/40-ORDERING-LIFECYCLE-RECONCILIATION.md) (status inventory, numeric-rider analysis, order-vs-delivery separation, transition graph, side-effects, source→target mapping, readiness matrix, evidence index).
- **P1.7.12 — Ordering Foundation: COMPLETE** (canonical Order create + native status lifecycle; **no schema change**). Implemented over the EXISTING `Order`/`OrderItem`/`OrderStatusEvent`: `OrderService.createOrder` (validates references, **merchant-tenant scope** via P1.7.2 `MerchantScopeService`, validates `menuItemId` belongs to the restaurant, computes **exact BigInt** money — `lineTotal = unitPrice×qty`, `subtotal = Σ lineTotal`, `grand = subtotal − discount + tax + fee + delivery` satisfying the `order_total_integrity` CHECK, initial status **INITIAL** per source `user-ordering.class.ts`), `transitionStatus` (validated native-lifecycle graph, rejects invalid/terminal transitions), `getOrder`. **Creation + each transition are transactional** (Order + OrderItems + initial `OrderStatusEvent`; status update + event). OrderItems capture **snapshots** (`nameSnapshot`/`variantSnapshot`, `unitPriceMinor`/`lineTotalMinor`, optional `menuItemId`). **Rider ON_THE_WAY/DELIVERED are native `OrderStatus`** on the single field (P1.7.11) — **no separate rider state machine**; `paymentStatus`/`fulfillmentStatus` stay separate defaults (not collapsed). Duplicate protection = existing `orderNumber @unique` (caller-supplied reference; no idempotency framework). Added `apps/api/src/modules/ordering/` (`OrderRepository` + `OrderService`; no controllers). **Payment/delivery/POS/realtime/cart/ONDC/frontend DEFERRED.** Docs: [domains/41-ORDERING-FOUNDATION.md](./domains/41-ORDERING-FOUNDATION.md). Validation: build/lint/format ✓, **220/220 tests** (210 prior + 10 new), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**. **NO FRONTEND WAS MIGRATED.**

- **P1.7.13 — Super Admin platform setup & merchant configuration reconciliation (checkpoint, discovery only).** Forensic trace across `amealio-vendordashboard` (backend), `amealiodashboardmvp-` (Merchant+Admin SPA), and `amealio_web_app` to establish the **configuration/setup dependency chain** before transactional commerce. **Validated the strategic hypothesis** (SuperAdmin defs → Onboarding → Subscription/config → Seating → Menu → Items → Celebrations/Experiences → Offers → transactional) against source: it is sound as a product rollout, but only a subset of arrows are HARD technical dependencies. **Key findings:** (1) SuperAdmin definitions HARD-gate onboarding but the India-first minimum is **already satisfied** (Category/Cuisine P1.7.4 + Currency P1.7.6); Mood/Craving/exp-events/media libraries are discovery-only and deferred. (2) The real chain gap is the **merchant operating foundation** — P1.7.10 creates a Merchant but does **not** provision an owner `StaffMember`/`StaffCredential` (`merchant-provisioning.service.ts:61–73`) and there is no activation/approval gate nor restaurant-profile/subscription **update** path, so a provisioned target merchant **cannot authenticate/operate** (legacy operating principal is `VendorUser`; refines doc 38: `have_vendor_submitted_details`/`has_admin_approved` is **admin-approval-driven server-side**, not FE). (3) `table_setup` is **embedded in the subscription doc** (`subscription.model.ts:408–461`, duplicated) → Subscription-config → Seating is HARD, and Seating carries an owner decision (normalize vs JSON+status sidecar). (4) **Menu→Items HARD; Seating→Menu NOT a technical dependency; Offers & Experiences independent/parallelizable and do NOT block Ordering** (offer optional at cart/order time). (5) P1.7.5 catalog is read-only — entire menu/item **write** layer missing; "no platform Item Catalog" (doc 34) **re-confirmed**. (6) No `Celebration` entity — celebrations = ExpEventManagement taxonomy + merchant-owned `Experience` + separate `Events`/scraped `exp_events`. (7) `Offer`/`Coupon`/`CouponRedemption` target schema exists; promotions API not started. **Recommended next slice: Merchant Owner Provisioning & Onboarding Activation** (provision owner `StaffMember`+`StaffCredential` at merchant create + activation gate + config-update writes; reuses P1.7.1D/E + P1.7.10; no schema change expected). Owner decisions registered (DEC-1 provisioning contract, DEC-2 table_setup target, DEC-3 item availability/tax/combos, DEC-4 subscription status/cardinality, DEC-5 attribute→feature mapping, DEC-6 cuisine source). **No implementation** (no code/schema/migration/test change; 220/220 unchanged). Docs: [domains/42-SUPERADMIN-MERCHANT-CONFIGURATION-RECONCILIATION.md](./domains/42-SUPERADMIN-MERCHANT-CONFIGURATION-RECONCILIATION.md).

- **P1.7.14 — Merchant Owner Provisioning & Activation Foundation: COMPLETE** (merchant operating foundation; **no schema change**). Resolves doc-42 DEC-1 from source and makes a P1.7.10-provisioned merchant able to become an authenticated, activated operational merchant, over the EXISTING identity (P1.7.1D/E/F) + merchant/subscription models. Added `apps/api/src/modules/onboarding/` `MerchantOwnerService` + `MerchantOwnerRepository`: (1) **owner provisioning** — one `StaffMember{staffRole: MERCHANT_OWNER, merchantId}` + PASSWORD `StaffCredential` (bcrypt via the shared hasher), created **atomically** (owner+credential in one tx), **SUPER_ADMIN-only**, server-derived merchant, one owner per merchant (dup → 409), initial status **BLOCKED** (= legacy `has_admin_approved=false`); (2) **activation** — owner status **BLOCKED↔ACTIVE**, SUPER_ADMIN-only, **enforced by the EXISTING staff auth** (`StaffAuthService.login`/`refresh`, `JwtStaffGuard` reject non-ACTIVE) — no new field/enforcement; (3) **restaurant-profile update** — merchant-scoped partial update of the evidenced fields (name/city/state/pinCode/country/timezone/currencyCode/lat/lon); (4) **subscription-config update** — merchant-scoped **non-destructive deep merge** of `config` (+status), unrelated keys preserved, `table_setup` NOT normalized (DEC-2 deferred). DEC-1 **RESOLVED** (owner=VendorUser→StaffMember(MERCHANT_OWNER)+StaffCredential; approval=owner status). Onboarding state (`onboardingStep`/`softOnboarding`/`onboardingSubmitted`) kept distinct from activation. **Limitation (deferred):** `StaffAccountStatus` lacks `PENDING`, so pending-approval = `BLOCKED`; public self-signup + multi-staff merchant-level activation deferred. **No second auth system, no controllers, no frontend, no seating/menu/item/offers/experiences/ordering.** Docs: [domains/43-MERCHANT-OWNER-PROVISIONING-ACTIVATION.md](./domains/43-MERCHANT-OWNER-PROVISIONING-ACTIVATION.md). Validation: build/lint/format ✓, **230/230 tests** (220 prior + 10 new incl. real staff-login activation gate), `prisma validate` ✓, `migrate status` up to date, **schema/migrations unchanged**. **NO FRONTEND WAS MIGRATED.**

- **P1.7.15 — Seating & table_setup reconciliation (checkpoint, discovery only): DEC-2 RESOLVED (Hybrid).** Forensic trace across `amealio-vendordashboard` + `amealiodashboardmvp-` + `amealio_web_app` with file:line evidence. **Findings:** legacy `table_setup` is embedded in the Subscription doc at **10 paths** (one logical shape `{standard,floors[],seat[],table[]}`; casual/multi-service seating + casual/fast-food/multi-service dine-in copies; hospitality none; closed Mongoose subdoc, unknown keys dropped) and is **MIXED config+inventory+runtime** — `table[].status` (`AVAILABLE|OCCUPIED|DIRTY|ON_HOLD|UNAVAILABLE`) is **runtime**, mutated by the Diner lifecycle (`diner.hooks.ts`), a manual merchant PATCH, and the auto-cancel helper. **Booking runtime** is a separate `Diner` collection: `service_type ∈ {SEATING,RESERVATION}`, walk-in vs waitlist = `isWalkIn` boolean, `diner_status = SEATING_STATUS{INITIAL,PENDING,NOTSEATED,SEATED,REJECTED,COMPLETED,CANCELLED}`; reservations are **capacity/restaurant-level at booking** and **physical-table-bound only at accept/seat**. **Seating↔Ordering = optional** (`cross_ref_id`/`diner_id`; occupancy is Diner-driven, not order-driven; target `Order` has no seating fields — correct). **Seating↔Experience/Event = SOFT/OPTIONAL**. **UNKNOWNs resolved:** `seatingarea` collection is marginal/bootstrap-only (superseded by `Sub Category` taxonomy); **QR table persistence NOT FOUND** (client-generated at print; only `restaurant.restaurant_qr` scan-pay image persisted); **no waiter↔table binding**. **Target:** `SeatingArea{name}`/`RestaurantTable{code,capacity}`/`SeatingRequest`(thin)/`ReservationBlock`/`OperatingHours` + `SeatingType`/`SeatingStatus` enums exist, but no seating module, no table-status enum/model, and `table_setup` lives only in `Subscription.config`. **DEC-2 = Option C (Hybrid):** Subscription.config keeps feature/config gates + timers/rules (unknown-preserving); dedicated entities own inventory+runtime+bookings (extend `SeatingArea`/`RestaurantTable`, add a `TableStatus`, use `SeatingRequest`). Residual sub-decisions (TableStatus placement + cron parity, dual seating/dine-in embed reconciliation, `manageDateRange` hours shape) do NOT block. **Seating does NOT block Menu/Items, Offers, Experiences, or Ordering.** **Recommended next slice: P1.7.16 bounded Seating implementation** (SeatingArea/RestaurantTable write from table_setup + SeatingRequest create for walk-in/waitlist/reservation, merchant-scoped; runtime status/cron parity SHOULD/deferred; waiter/QR/experience-event FUTURE). **No implementation** (no code/schema/migration/test change; 230/230 unchanged). Docs: [domains/44-SEATING-TABLE-SETUP-RECONCILIATION.md](./domains/44-SEATING-TABLE-SETUP-RECONCILIATION.md).

- **P1.7.16 — Merchant Seating Configuration & Seating Request Foundation: COMPLETE** (one **additive** migration `20260902093000_p1_7_16_seating_inventory`). Implements the DEC-2 hybrid: normalized seating **inventory** + physical-table **runtime status** + **booking request** foundation over the EXISTING seating models; feature gates/timers/rules stay in `Subscription.config`. Added `apps/api/src/modules/seating/` `SeatingService` + `SeatingRepository`: `createSeatingArea`/`createTable`/`listTables` (inventory; `@@unique([restaurantId,name])` + `@@unique([seatingAreaId,code])`), `setTableStatus` (RUNTIME `TableStatus` = legacy manual table PATCH), `createSeatingRequest` (WALK_IN/WAITLIST/RESERVATION; PENDING; reservationAt required for RESERVATION; **no** physical table at creation) + `updateSeatingRequest` (minimal: status + physical-table binding at accept/seat, same-restaurant enforced). **Schema (additive):** `TableStatus` enum; `SeatingArea` += legacyId/deletedAt/unique(restaurantId,name); `RestaurantTable` += legacyId/name/floor/shape/isActive/status/timestamps/deletedAt. Legacy `table_setup` fields with no confirmed target consumer (number/location/temporay/reserveDate) and `seat[]` are **documented, not mapped**. Merchant-tenant-scoped (P1.7.1F/P1.7.2); SUPER_ADMIN explicit target; cross-merchant/deleted/unknown rejected; activation gate (P1.7.14) upstream. **Boundary preserved:** RESERVATION/WALK-IN/WAITLIST vs physical-table-assignment vs table-occupancy kept distinct. **Deferred:** booking→table-status auto-sync + auto-cancel cron (no scheduler invented), seats, waiter, QR, experience/event seating, order cross-links (Seating→Ordering optional; P1.7.12 untouched). Docs: [domains/45-SEATING-CONFIGURATION-REQUEST-FOUNDATION.md](./domains/45-SEATING-CONFIGURATION-REQUEST-FOUNDATION.md). Validation: build/lint/format ✓, **242/242 tests** (230 prior + 12 new incl. real staff-login activation gate + non-destructive config merge), `prisma validate` ✓, `migrate status` up to date (7 migrations; applied dev+test). **NO FRONTEND WAS MIGRATED.**

- **P1.7.17 — Menu & Item write reconciliation (checkpoint, discovery only).** Forensic trace (frontend→API→service→persistence, file:line) of the legacy merchant/admin menu+item WRITE flow to scope P1.7.18 and resolve DEC-3. **Findings:** legacy hierarchy = `Menu`(custom; "standard" menu is virtual) + `menuCategory`(sections) + `vendorItems`(items), written via `/menu`, `/menu-category`(+`/menuCategory/order`), `/vendor-items`. **CONFIRMED** `menuCategory→MenuSection` (merchant section, not platform taxonomy — platform `Category`/`Sub Category` are string-label pickers) and `vendorItems→MenuItem(+ItemVariant+ItemChannelConfig+AddOnGroup/AddOn)`; the existing target hierarchy is the right shape. **DEC-3:** ITEM AVAILABILITY **RESOLVED** — three legacy layers untangled: `status`(Boolean)=publication gate (→ separate target publish flag, NOT the enum), `availability` enum=stock (→ `MenuItem.availability` 1:1), `checkIfOpen()`+weekly/`date_of_availability`=runtime overlay (DEFERRED). CATEGORY/TAX **PARTIALLY RESOLVED** — canonical tax = item per-channel `sur_charges` → existing `ItemChannelConfig.surcharges Json` (**preserve**); `menuCategory.charges[]`+`price_include_tax` = owner decision (additive `MenuSection.charges Json?` vs propagate-only); subscription tax stays in `Subscription.config`; no tax engine. COMBOS **UNRESOLVED** — live merchant-owned feature (separate `Combo` collection, own per-channel pricing, refs vendorItems), **no target model** → owner decision (first-class Combo vs composite MenuItem vs defer; recommend defer). **"No platform Item Catalog" RE-CONFIRMED** (`catalogue`/`chaincatalogue` group `vendorItems` refs; global items are `vendorItems is_global` cloned to merchant rows on import). **POS** optional/deferred (preserve `posItemId`). **Ownership:** merchant+restaurant-scoped (P1.7.1F/P1.7.2); Menu(merchant+restaurant), sections/items/children via chains. **Dependencies:** Restaurant + Merchant activation HARD; Subscription/Categories/Cuisine/Currency SOFT/OPTIONAL; Seating/Offers/Experiences/Ordering/Payment NO DEPENDENCY. **Target gaps:** publication flag, section charges, weekly/date schedule, variant `isDefault`/`available`, per-variant channel price, dietary/nutrition, combos. **Recommended P1.7.18:** merchant-scoped `CatalogWriteService` over EXISTING models (Menu/MenuSection/MenuItem/ItemVariant/ItemChannelConfig/AddOnGroup/AddOn) + small additive fields (`MenuItem.isPublished`, optional `Menu`/`MenuSection.description`, variant flags, section charges); no customer UI/ordering/POS/combos. **No implementation** (no code/schema/migration/test change; 242/242 unchanged). Docs: [domains/46-MENU-ITEM-WRITE-RECONCILIATION.md](./domains/46-MENU-ITEM-WRITE-RECONCILIATION.md).

- **P1.7.18 — Merchant Menu & Item Write Foundation: COMPLETE** (one **additive** migration `20260902100500_p1_7_18_menu_item_write`). Merchant-scoped `CatalogWriteService` + `CatalogWriteRepository` (in the existing catalog module) over the EXISTING hierarchy: **Menu** create/update (name/description/type/visibility/legacyId), **MenuSection** create/update/**reorder** (name/description/sortOrder/categoryId?), **MenuItem** create/update (name/description/availability/**isPublished**/menuSectionId/posItemId?/legacyId) with **atomic** nested variants/channel-configs/add-on groups, **ItemVariant** create/update (size/uomId/priceMinor/pax/**isDefault**/**available**), **ItemChannelConfig** **upsert** (channel/enabled/priceOverrideMinor/surcharges Json), **AddOnGroup**/**AddOn** create/update. **DEC-3 publication⊥availability enforced:** new `MenuItem.isPublished` (legacy `status`) is distinct from `availability` (stock). **Schema (additive):** `MenuItem.isPublished`, `ItemVariant.isDefault`/`available`, `Menu.description`, `MenuSection.description`. Merchant-tenant-scoped (P1.7.1F/P1.7.2): server-derived scope, cross-merchant/cross-restaurant/deleted/unknown rejected, child writes resolve up the chain, SUPER_ADMIN explicit target, activation gate (P1.7.14) upstream. Item+children creation is one atomic nested write (rollback verified). **DEFERRED/OWNER DECISION:** combos (no model), `menuCategory.charges`/tax engine, per-variant channel pricing, scheduled/date availability, POS sync, dietary/nutrition/media, HTTP controllers/frontend. Docs: [domains/47-MENU-ITEM-WRITE-FOUNDATION.md](./domains/47-MENU-ITEM-WRITE-FOUNDATION.md). Validation: build/lint/format ✓, **251/251 tests** (242 prior + 9 new incl. atomic-rollback + activation gate), `prisma validate` ✓, `migrate status` up to date (8 migrations; applied dev+test). **NO FRONTEND WAS MIGRATED.**

- **P1.7.19 — Celebrations / Experiences reconciliation (checkpoint, discovery only).** Forensic trace (frontend→API→service→persistence, file:line) across `amealio-vendordashboard` + `amealiodashboardmvp-` + `amealio_web_app`, plus the added **Custom Menu** and **Media Dashboard** forensics. **Findings:** **Experience** (`experiences`, merchant-owned, `restaurantId` req) is booked via **`expRequest`**; **Events** (`events`) is booked via **`eventHandler`** — fully separate domains. **"Festival" has no backend entity** (UI/taxonomy only). **`exp-events`(`ExpEventManagement`) ≠ `exp_events`**: the former is a SuperAdmin **taxonomy** (`type EVENT|EXPERIENCE`, FK Sub Category) driving the UI; the latter is a **scraped external-events** read-only catalog. **"Celebration" is a UI label** for `ExpEventManagement type=EXPERIENCE` rows → lists merchant Experiences by subCategory. Booking lifecycle: cart→checkout(`expRequest` INITIAL)→pay(Razorpay immediate)/pay-at-site(deferred)→PENDING/NOTSEATED→materialize **Diner**(non-delivery)+**Order**(when food/not occasion-text)→vendor progression→cancel+refund. Food **configurable** (included/extra/none); seating date/pax required, physical table assigned later; **capacity enforced for CURATED** at booking. **Custom Menu = merchant-owned `Menu(type=CUSTOM)`** referenced by `Experience.CustomMenu[]` (reference, not snapshot) — target `Menu`/`MenuSection`/`MenuItem` (P1.7.18) represents it; gap = Experience↔Menu link. **Media Dashboard = 4 separate legacy patterns** (merchant upload→embedded URLs; platform `experience_catalog` templates; unshipped `media-catalogue` backend; `upload-assets` S3 pipe; embedded icon/photo fields) — target has **no media entity/columns**. **Target: entire Experience/Event/Media domain is a CONFIRMED GAP** (no Experience/Event/Media models); reuse = Category/Currency/Menu(CUSTOM)/SeatingRequest/PaymentIntent/User. **Owner decisions:** model Experience first-class (recommend yes for P1.7.20), booking model, Experience↔Menu link, taxonomy overlay, defer scraped exp_events + merchant Events, media representation (embedded URLs first). **Recommended P1.7.20 = Merchant Experience Configuration Foundation** (first-class `Experience` + Experience↔CustomMenu ref; defer booking/payment/refund/Diner/Order/realtime/taxonomy-overlay/scraped-events/Events/media-catalogue/customer-UI). **No implementation** (no code/schema/migration/test change; 251/251 unchanged). Docs: [domains/48-CELEBRATIONS-EXPERIENCES-RECONCILIATION.md](./domains/48-CELEBRATIONS-EXPERIENCES-RECONCILIATION.md).

- **P1.7.20 — Merchant Experience Configuration Foundation: COMPLETE** (one **additive** migration `20260902104500_p1_7_20_experience_configuration`). New target **`Experience`** + **`ExperienceMenu`** models + 4 enums (`ExperienceType FOOD|EVENT`, `ExperienceKind SPECIAL|CURATED`, `ExperienceFoodMode`, `ExperienceMenuMode`) with virtual back-relations on Merchant/Restaurant/Category/Menu. Added `apps/api/src/modules/experience/` `ExperienceService` + `ExperienceRepository`: create/get(by id+legacyId)/list-by-restaurant/update/**publish**/**unpublish**/**setCustomMenus**/soft-delete. **Source-backed config:** type/expType, Category/Sub Category FKs (reuse P1.7.4), capacity (`totalSeats`/`minSeats`/`maxSeats` — config only; `seatsLeft` runtime **deferred**), distinct **BigInt minor-unit** prices (listing/adult/kids/occasion), `currencyCode` (default INR; P1.7.6), foodMode/menuMode + food/occasion text, `startAt`/`endAt` + `scheduleConfig Json?` (no scheduling engine), `active`/`isDraft` publication, `deletedAt`. **Custom Menu relationship:** `ExperienceMenu` join — one Experience → many CUSTOM menus of the SAME restaurant with ≤1 default (legacy `CustomMenu[]`/`defaultCustomId`); STANDARD/foreign/>1-default rejected; reference not snapshot. Merchant-tenant-scoped (P1.7.1F/P1.7.2): server-derived scope, cross-merchant/deleted/unknown rejected, SUPER_ADMIN explicit target, activation gate (P1.7.14) upstream. **DEFERRED:** booking(`expRequest`)/payment/refund/Diner/Order, media, packages, `classification[]`, ExpEventManagement overlay, scraped `exp_events`, merchant Events, customer UI, HTTP controllers. Docs: [domains/49-EXPERIENCE-CONFIGURATION-FOUNDATION.md](./domains/49-EXPERIENCE-CONFIGURATION-FOUNDATION.md). Validation: build/lint/format ✓, **260/260 tests** (251 prior + 9 new incl. activation gate + custom-menu tenancy), `prisma validate` ✓, `migrate status` up to date (9 migrations; applied dev+test). **NO FRONTEND / booking / payment / Event / Media WAS IMPLEMENTED.**

- **P1.7.21 — Offers/Promotions reconciliation (checkpoint, discovery only).** Forensic trace (frontend→API→service→persistence, file:line) across `amealio-vendordashboard` + `amealiodashboardmvp-` + `amealio_web_app`. **Findings:** legacy **Offer and Coupon are ONE entity** — the `Offers` collection with a unique `coupon_code` field + embedded `offerUsedBy[]`/`offerUsed` (no separate Coupon/CouponRedemption collection). Discount = **percentage or fixed, order-level, capped** by `maximum_discount_applied` (no item/category/menu/experience/BOGO/free-item). **Discount is computed FRONTEND** (`OrderingCalculations.js`) + legacy cart (`usercart.class.ts`); **order create does NOT recompute** (trusts client `total_amount`) — integrity risk. Usage is persisted **twice** (coupon-apply `POST /user/offers` + payment) and reversed on **cancel** (not standalone refund). Offers apply at **order/restaurant/vendor** level + `service_type[]`; **experience** offer FK is dormant/unused (browse-only). Eligibility ENFORCED: min-order, order-type, date window, per-user (global); NOT item/category/experience/payment-method. **Adjacent systems are INDEPENDENT** of core Offers: `merchant-permotion` (curation), `promotional-event`/`promotionsvideo` (marketing), `referral_program`/`referral-code`/`referralService`/`SignupReward` (wallet economy). **Subscription `offer_management` gates UI only** (not backend). **Target `Offer`/`Coupon`/`CouponRedemption` EXIST but INCOMPLETE** (missing usage limits/counters, order-amount gates, min/max discount, active/hidden, per-user limits, multi-restaurant, media/T&C, discount mutual-exclusivity, order discount snapshot); `ReferralProgram` is a Json placeholder (wrong semantics); **no promotions API module**. The Offer→Coupon 1:N split is faithful for legacy 1:1 data. **Owner decisions:** discount source-of-truth (recommend server-side), extend Offer/Coupon schema, redemption timing, SPLIT settlement, defer referral/curation/video, subscription gate enforcement. **Recommended P1.7.22 = Merchant Offer & Coupon Configuration Foundation** (merchant-scoped Offer+Coupon write over the existing models extended additively; defer redemption/discount-calculation/order-payment integration/referral/curation/video). **No implementation** (no code/schema/migration/test change; 260/260 unchanged). Docs: [domains/50-OFFERS-PROMOTIONS-RECONCILIATION.md](./domains/50-OFFERS-PROMOTIONS-RECONCILIATION.md).

- **P1.7.22 — Merchant Offer & Coupon Configuration Foundation: COMPLETE** (one **additive** migration `20260902153700_p1_7_22_offer_configuration`). Merchant-scoped `OfferService` + `OfferRepository` (new `apps/api/src/modules/offer/`) to **configure** an Offer definition + its coupon code over the EXISTING `Offer`(+`Coupon`) models: create/get/list(merchant|global)/update/**activate-deactivate**/soft-delete. **Configuration ONLY** — `CouponRedemption` **unused**; no redemption, no discount calculation, no usage counters, no SPLIT calc, no order/payment integration, no cron, no controllers. **Discount:** percentage (1..100) XOR fixed (BigInt>0), + `maxDiscountMinor` cap. **Order gates:** min/max order minor (config, not enforced at order time). **Usage config:** `maxUsageLimit`/`perUserLimit`/`useLimit`/`useFrequency` (no counters). **Validity:** validFrom/validTo + `active` (default false; no cron). **serviceTypes** = free-form Json string array (no enum). **settlementType** MERCHANT|ADMIN|SPLIT (SPLIT stored not calculated). **Coupon code** held in the existing `Coupon` (one per offer, `code @unique`, optional). **Scope:** `isGlobal` = SUPER_ADMIN-only platform-wide; non-global merchantId server-derived; single optional restaurant (validated to merchant); multi-restaurant deferred. **Schema (additive):** `Offer` += legacyId/description/termsAndConditions/active/maxDiscountMinor/min+maxOrderMinor/maxUsageLimit/perUserLimit/useLimit/useFrequency; `Coupon`/`CouponRedemption` unchanged. Merchant-tenant-scoped (P1.7.1F/P1.7.2); activation gate (P1.7.14) upstream. Docs: [domains/51-OFFER-COUPON-CONFIGURATION-FOUNDATION.md](./domains/51-OFFER-COUPON-CONFIGURATION-FOUNDATION.md). Validation: build/lint/format ✓, **268/268 tests** (260 prior + 8 new incl. global-scope authz + activation gate), `prisma validate` ✓, `migrate status` up to date (10 migrations; applied dev+test). **NO redemption / discount-calculation / order / payment / frontend was implemented.**

- **P1.7.23 — Cart/Offer/Order runtime reconciliation (checkpoint, discovery only).** Forensic trace (frontend→API→service→persistence, file:line) of the legacy commercial runtime + target assessment. **Findings:** legacy runs **three cart layers** (legacy `/usercart` server-calc, new `/user/cart`+`/checkout` with **no offer wiring**, and client `sessionStorage`+`OrderingCalculations.js`); the web checkout path is **client-supplied** — `POST /user-ordering` **persists client discount/total without recomputation** and payment charges the stored total. Offer usage (embedded `Offers.offerUsed`/`offerUsedBy[]`; **no CouponRedemption collection**) is incremented at **apply** (`/user/offers`, pre-payment) **and** at **payment** → **double-count**; cancel decrements one; **refund does not reverse**; **no idempotency/concurrency guards** (legacy). Experience promotion is **DORMANT** (`experience_cart.offer` FK unused); seating has **no** offer runtime dependency. **Target:** `Order` (P1.7.12) has money fields + grand-total CHECK + `couponRedemptions` relation but **no offer link/settlement**; `PaymentIntent`/`PaymentAttempt` provide **idempotency the legacy lacks** (no payment module yet); `CouponRedemption` is **minimal** (no status/reversal/unique/paymentIntent link); `Cart`/`CartItem` are **schema-only** (no price/offer fields, no module). **DEC-OFF-1 RESOLVED:** legacy = client-supplied (A); **target = server-authoritative discount at order creation (D)** — never trust client discount/total (security fix). **DEC-OFF-3 RESOLVED:** legacy = split/inconsistent double-count; **target = one idempotent `CouponRedemption` at the commit point** (payment capture when payment exists; **order placement** in the interim), usage **derived** from non-reversed redemptions, **reversed on cancel + refund**. **Required (documented, not implemented):** extend `CouponRedemption` (status/reversedAt + unique + discount snapshot + optional paymentIntent), optional `Order`↔coupon link, a redemption service; Cart runtime **optional** (order-create already takes resolved input). **Recommended P1.7.24 = Offer Redemption & Server-Side Discount at Order Creation** (extend P1.7.12 order-create: validate coupon server-side, compute discountTotalMinor/grandTotalMinor, write one idempotent reversible CouponRedemption; defer Cart/payment-capture-timing/settlement/experience). **No implementation** (no code/schema/migration/test change; 268/268 unchanged). Docs: [domains/52-CART-OFFER-ORDER-RUNTIME-RECONCILIATION.md](./domains/52-CART-OFFER-ORDER-RUNTIME-RECONCILIATION.md).

- **P1.7.24 — Offer Redemption & Server-Side Discount at Order Creation: COMPLETE** (one **additive** migration `20260902160000_p1_7_24_offer_redemption_discount`). Extends the EXISTING P1.7.12 order-create path so an applied coupon makes the **server authoritative** for the discount and grand total (**DEC-OFF-1**). `OrderService.createOrder` now accepts an optional **`couponCode`** (intent only); the ordering repo resolves `Coupon.code → Offer` (`findAppliedOfferByCouponCode`) and pure functions (`offer-discount.ts`) validate eligibility (**active**, not soft-deleted, `validFrom/validTo` window, restaurant/merchant/**global** scope, `minOrderMinor`/`maxOrderMinor` gates vs subtotal, **service type** matched to canonical `OrderType`/`ALL`, coupon match) and compute the discount (**percentage** `floor(subtotal×pct/100)` / **fixed**, capped by `maxDiscountMinor`, clamped to subtotal). The **client-supplied discount/total are ignored** when a coupon is present; the server persists `discountTotalMinor` + derived `grandTotalMinor` (order_total_integrity CHECK preserved) + `offerId`/`couponId`. **Redemption ledger:** `CouponRedemption` extended additively — `RedemptionStatus {ACTIVE,REVERSED}`, `discountAppliedMinor` (snapshot), `reversedAt`, **`@@unique([couponId,orderId])`**, `@@index([userId])`. **Usage** is **derived** from ACTIVE redemptions (no `offerUsed`/`offerUsedBy` counters): `maxUsageLimit` (total) + `perUserLimit` (per-user) enforced **inside the order transaction** after a per-coupon **`SELECT … FOR UPDATE`** lock → **no oversubscription** under concurrency (**DEC-OFF-3**). **Idempotency:** `orderNumber @unique` (order retries roll back the whole tx) + `(couponId,orderId)` unique (redemption). **Reversal:** the existing `→ CANCELLED` transition reverses the redemption (status REVERSED + `reversedAt`) and releases usage; **refund reversal deferred**. **Payment untouched** — commit point = **order placement**. Added `apps/api/src/modules/ordering/domain/offer-discount.ts` + `RedemptionDirective`/`AppliedOffer`/`RedemptionRecord` types; `OrderRepository` extended; **no new controller/module import** (P1.7.22 `OfferService` untouched). **Deferred (P1.7.25):** `useLimit`/`useFrequency` per-period enforcement (ambiguous semantics), code-less-offer application, refund reversal, payment-capture commit point + paymentIntent linkage, settlement/SPLIT. Docs: [domains/53-OFFER-REDEMPTION-DISCOUNT-FOUNDATION.md](./domains/53-OFFER-REDEMPTION-DISCOUNT-FOUNDATION.md). Validation: build/lint/format ✓, **293/293 tests** (268 prior + 25 new incl. server-authoritative discount, client-inflated/reduced ignored, concurrent no-oversubscribe, idempotent retry, cancel-reversal), `prisma validate` ✓, `migrate status` up to date (11 migrations; applied dev+test). **NO payment/refund/Experience-booking/frontend was implemented; P1.7.18/P1.7.20/P1.7.22 untouched.**

- **P1.7.25 — Order Cancellation / Coupon Redemption Reversal + Usage Semantics Reconciliation: COMPLETE** (**no schema change; no migration**). **Cancellation reversal hardened:** `OrderRepository.updateStatusWithEvent` now performs a **compare-and-set** status update (`updateMany WHERE id=? AND status=<fromStatus>`) so that the `OrderStatusEvent` and the `ACTIVE→REVERSED` `CouponRedemption` reversal occur **exactly once** and consistently under concurrent cancellation — all in the SAME transaction as the `→ CANCELLED` change (never `Order=CANCELLED` with an `ACTIVE` redemption, nor the reverse). The reversal (introduced P1.7.24) reuses the EXISTING `OrderService.transitionStatus` cancellation path (merchant-tenant-scoped, terminal-state-guarded) — **no parallel cancellation API**. Usage stays **derived** from `COUNT(status=ACTIVE)` (no `offerUsed`/`offerUsedBy` counters); cancellation **releases** `maxUsageLimit` + `perUserLimit`. Reversal is scoped by `orderId` (never touches another order's redemption) and is idempotent (terminal `CANCELLED` rejects re-cancel upstream; `status=ACTIVE` predicate prevents double-reverse). **Refund/payment reversal deferred** (target has no payment lifecycle). **`useLimit`/`useFrequency` reconciliation → CASE B (deferred):** legacy `usercart.class.ts:1244-1254` + `user-offer.class.ts:257-268` + `FREQUENCY_TIME{DAILY:day,WEEKLY:week,MONTHLY:month,YEARLY:year}` establish the RULE = **per-user redemptions within the current `useFrequency` calendar period** (global-gated in legacy), but the **calendar timezone anchor** (moment server-local vs target UTC/IST) and **week-start convention** are NOT source-pinned → enforcing would invent reset-window semantics, so the fields are **preserved but not enforced** (owner decisions OD-USG-1/2/3); P1.7.24 behavior unchanged. Docs: [domains/54-OFFER-REDEMPTION-REVERSAL-USAGE-FOUNDATION.md](./domains/54-OFFER-REDEMPTION-REVERSAL-USAGE-FOUNDATION.md). Validation: build/lint/format ✓, **305/305 tests** (293 prior + 12 new incl. reversal, atomicity, no-double-reverse, other-order isolation, usage release, concurrent cancellation), `prisma validate` ✓, `migrate status` up to date (11 migrations; no new migration). **NO payment/refund/Experience-booking/frontend was implemented; P1.7.12/P1.7.18/P1.7.20/P1.7.22/P1.7.24 behavior preserved.**

- **P1.7.26A — Offer Use-Limit / Use-Frequency Reconciliation: RECONCILIATION COMPLETE** (discovery only; **no code/schema/migration/test change**). Resolves the three usage-frequency owner decisions left open by P1.7.25, from legacy source (`amealio-vendordashboard`) + customer (`amealio_web_app`) + target. **OD-USG-1 (calendar timezone anchor) RESOLVED = `Asia/Kolkata` (IST, UTC+05:30):** `process.env.TZ = "Asia/Kolkata"` at `app.ts:26`/`index.ts:12`, and the offer window uses bare `moment()` (v2.29.0, `usercart.class.ts:1236-1237`, `user-offer.class.ts:248-249`) with **no** `.tz`/`moment-timezone`/restaurant-timezone → periods computed in IST; recommend an explicit IST anchor (NOT UTC, which would shift resets by 5.5h). **OD-USG-2 (WEEKLY week-start) RESOLVED = Sunday:** moment default `en` locale with **no** `moment.locale`/`updateLocale`/`defineLocale`/`dow` override anywhere → `startOf('week')` = Sunday; recommend Sunday→Saturday (NOT ISO Monday). **OD-USG-3 (gating) RESOLVED = global-only:** per-user (`maxUsage`) + per-period (`useLimit`/`useFrequency`) checks are wrapped in `if (isGlobal)` in **both** the cart-apply (`usercart.class.ts:1234`) and offer-listing (`user-offer.class.ts:242`) paths; `maximum_usage_limit` (global total) applies to all offers; the order-create path does not re-check; the customer app has no frequency logic. **Legacy rule:** an authenticated user may redeem a GLOBAL offer ≤ `useLimit` times within the current `useFrequency` IST calendar period (DAILY/WEEKLY(Sun-start)/MONTHLY/YEARLY, `moment().startOf/endOf`). **Usage lifecycle:** legacy double-records usage (coupon-apply `user-offer.class.ts:87-107`/`offers.hooks.ts:346-357` + payment-success `user-ordering.class.ts:3523-3530`) — P1.7.24's single `CouponRedemption` at the commit point eliminates this; **cancellation** releases one slot (`order_status===CANCELLED` → splice `offerUsedBy` + `offerUsed-1`, `user-ordering.class.ts:3286-3308`/`ordering.class.ts:6302-6313`/`autoCancel.ts:796-806`) — target = `ACTIVE→REVERSED` (P1.7.25); **standalone refund** and **payment failure** do NOT release usage (legacy leak) → target defers refund reversal (OD-REF-1). **Target mapping:** the rule is fully representable via derived counts over `CouponRedemption(status,createdAt,userId,couponId)` + `Offer.useLimit`/`useFrequency` with **no schema change** and **no mutable `offerUsed`/`offerUsedBy` counters**; the existing P1.7.24 per-coupon `SELECT … FOR UPDATE` lock is **sufficient** to serialize the windowed per-user check (cancel-vs-redeem is fail-closed/conservative). Residual = **target design choices** (adopt legacy global-only gating vs P1.7.24's all-offers `perUserLimit` strengthening; boundary inclusivity; fixed-IST vs future per-restaurant timezone) + **OD-REF-1**. Docs: [domains/55-OFFER-USAGE-FREQUENCY-RECONCILIATION.md](./domains/55-OFFER-USAGE-FREQUENCY-RECONCILIATION.md). **No implementation** (305/305 unchanged). Recommends **P1.7.26B = usage-frequency enforcement** (IST + Sunday, derived from ACTIVE redemptions, under the existing lock; no payment/refund).

- **P1.7.26B — Offer Usage-Frequency Enforcement: COMPLETE** (**no schema change; no migration**). Implements the P1.7.26A reconciliation (doc 55): `useLimit`/`useFrequency` are now enforced at order creation as a **per-user, per-calendar-period** redemption cap — **only for global offers** (`isGlobal`), preserving the legacy gate (OD-USG-3). Period windows are anchored to **Asia/Kolkata (IST, UTC+05:30)** with **Sunday-start** weeks (OD-USG-1/OD-USG-2), computed by the pure helper `apps/api/src/modules/ordering/domain/usage-frequency.ts` (`istUsagePeriodWindow`; DAILY/WEEKLY/MONTHLY/YEARLY; fixed +05:30 offset, no new date library). Usage is **derived** from `COUNT(CouponRedemption WHERE couponId, userId, status=ACTIVE, createdAt ∈ [periodStart, nextPeriodStart))` — **no mutable counters**; the check runs inside the existing order-creation `prisma.$transaction`, **after** the existing per-coupon `SELECT … FOR UPDATE` lock, so concurrent same-coupon redemptions serialize and cannot oversubscribe a period slot. Rejection reuses the existing usage error contract (`ConflictException`, `'Offer usage frequency limit reached for this period'`). `perUserLimit`, `maxUsageLimit`, and non-global offer behavior are **unchanged and kept distinct**. **Cancellation** (P1.7.25 `ACTIVE→REVERSED`) naturally releases a period slot (REVERSED rows are excluded); **refund/payment-failure** usage release remains **deferred** (OD-REF-1) — no payment/refund/cancellation code changed. Files: `ordering/domain/usage-frequency.ts` (new), `ordering/domain/ordering.types.ts` (+`useLimit`/`useFrequency` on `AppliedOffer`; +`isGlobal`/`useLimit`/`useFrequency` on `RedemptionDirective`), `ordering/infrastructure/order.repository.ts` (select + enforcement), `ordering/application/order.service.ts` (thread directive). Generated SQL verified via Prisma query logging. Docs: reconciliation in [domains/55-OFFER-USAGE-FREQUENCY-RECONCILIATION.md](./domains/55-OFFER-USAGE-FREQUENCY-RECONCILIATION.md) (**P1.7.26A = reconciliation; P1.7.26B = implementation**). Validation: build/lint/format ✓, **323/323 tests** (305 prior + 18 new: 7 pure IST-boundary + 11 enforcement incl. concurrency), `prisma validate` ✓, `migrate status` up to date (11 migrations; no new migration). **NO payment/refund/cancellation/schema change; non-global offers untouched.**

- **P1.7.27 — Payment / Razorpay / Refund Runtime Reconciliation: RECONCILIATION COMPLETE** (discovery only; **no code/schema/migration/test change**). Forensic trace (file:line) across `amealio-vendordashboard` + `amealio_web_app` + target of the **ordering** payment/refund/wallet/transaction/settlement runtime. **Legacy = client-orchestrated Razorpay**: order created first (`checkout.class.ts`, `payment_status=PENDING`), then `POST /razorpay?order` (amount ×100 → paise, `notes={orderId}`, ids on `transactional`), client SDK, `POST /razorpay?payments` (`payments.fetch`→`capture`), `PATCH /user-ordering` success keyed on **hardcoded `payment_status==1`**. **NO server-side signature/amount/order verification** (`razorpay.class.ts:1238-1298`); the Razorpay **webhook is a no-op stub** (`webhook.class.ts:27-28`) — confirmation is entirely client-driven. **Ordering refunds are wallet-only** (`wallet.create` refund branch → credit user `Wallet` + `transactional` `t_type=REFUND_AND_CANCELLATION`; Razorpay refund API used only by Experience/ONDC), **cancel-gated** (`order_status===CANCELLED` + `settleAmount>0`), via **≥8 duplicated `RefundOrder`** with **inconsistent amount formulas** and **no idempotency key** (`refundCompleted` + transition check only). **Coupon reversal is cancel-gated, not refund-gated** (confirms P1.7.25/26A); legacy also **double-counts** coupon usage (apply + payment). **Wallet** = mutable `balance` number, **no ledger, no locking** (last-write-wins). **`t_type`** (16-value ledger category) is **distinct** from **`transaction_type`** (3-value rail WALLET/RAZORPAY/SCAN_AND_PAY). **Settlement** = deferred (~T+2 `settleAfter`) vendor RazorpayX payout, commission via restaurant `comissionCode` %, ADMIN vs VENDOR offer split. **All numeric enums (`payment_status`/`payment_method`/`t_type`/`WALLET_ROLE`) are `process.env`-injected** (`config/default.js:746-953`) — symbolic vocabulary + ordering known, **integers absent from source**. **Target payment schema EXISTS but is UNUSED** (no `apps/api` payment module) and is **more robust than legacy**: `PaymentIntent`/`PaymentAttempt` (`idempotencyKey`/`razorpayPaymentId @unique`), idempotent `WebhookEvent` (`providerEventId @unique`), `WalletEntry` ledger (`balanceAfterMinor`), explicit `Transaction.direction`, `Refund.status`, `Settlement`/`SettlementItem`/`Payout` (`schema.prisma:71-135,230-240,1055-1213`). **Owner decisions:** **DR-02b/c/d/e remain BLOCKED — OWNER/DATA** (integers + semantic mapping need an env/DB census; note dual payment-status representation and 16→6 `t_type` collapse and USER-only target wallet vs legacy USER/VENDOR/SUPER_ADMIN roles); **OD-REF-1 PARTIALLY RESOLVED** — keep the P1.7.24 **order-placement commit point** until a verifiable payment module exists, refund-driven reversal deferred. Docs: [domains/56-PAYMENT-REFUND-RUNTIME-RECONCILIATION.md](./domains/56-PAYMENT-REFUND-RUNTIME-RECONCILIATION.md). **No implementation** (323/323 unchanged). Recommends **P1.7.28 = Payment Intent & Verified-Capture Foundation** (create PaymentIntent + Razorpay order + **signature-verified** capture + **idempotent WebhookEvent** ingestion + PaymentAttempt/Transaction over the existing schema; **no** refund/wallet/settlement, **no** commit-point move; greenfield writes use target enums, so DR-02b/c/d/e block only historical data migration).

- **P1.7.28 — Payment Intent & Verified-Capture Foundation: COMPLETE** (**no schema change; no migration**). New `apps/api/src/modules/payment/` over the EXISTING `PaymentIntent`/`PaymentAttempt`/`Transaction`/`WebhookEvent` (doc 56 §27 confirmed sufficient). **`PaymentService.createIntent`** derives `amountMinor`/`currencyCode` from `Order.grandTotalMinor` (server-authoritative; idempotent per `razorpayOrderId @unique`), `method=RAZORPAY`, `status=CREATED`. **`verifyAndCapture`** performs **server-side** verification — signature `HMAC_SHA256(order_id|payment_id, key_secret)` (constant-time), known-intent lookup, and **exact BigInt** amount + currency equality — then in one `prisma.$transaction` creates `PaymentAttempt` (`status=CAPTURED`, `razorpayPaymentId @unique`, `idempotencyKey @unique`), compare-and-sets `PaymentIntent → CAPTURED` (never backward), and creates one `Transaction` (`type=PAYMENT`, `direction=CREDIT`). **`RazorpayWebhookService.ingest`** verifies the **raw-body** HMAC (`RAZORPAY_WEBHOOK_SECRET`; bootstrap sets `rawBody:true`), persists `WebhookEvent` (`providerEventId @unique`) **idempotently** (redelivery = no-op), and processes `payment.captured` (shared `razorpayPaymentId @unique` guard ⇒ client-handoff + webhook = **one** transaction) / `payment.failed` (compare-and-set FAILED). **Client success flags are never trusted**; **no** `Transaction` on failed verification. Closes the legacy no-verification + stub-webhook gaps (doc 56 §8/§D). Idempotency is **DB-enforced** (no app locks); concurrent capture → exactly one transaction. New writes use **canonical target enums** (legacy numeric `payment_status`/`payment_method`/`t_type` not reproduced). Gated by `PAYMENTS_ENABLED`; Razorpay secrets are **config-only** (dev defaults; infra-managed in prod; never logged/committed). Routes: `POST /api/v1/payments/intents`, `/payments/verify`, `/payments/razorpay/webhook`. **DEFERRED:** refund, wallet, `WalletEntry`, settlement, `Payout`, live Razorpay `orders.create`, `Order.paymentStatus` sync, coupon reversal / **commit-point relocation (OD-REF-1 unchanged — stays order placement)**, and **all historical payment/wallet/settlement migration**; **DR-02b/c/d/e remain BLOCKED — OWNER/DATA** (block only historical data migration, not these greenfield writes). Docs: [domains/57-PAYMENT-VERIFIED-CAPTURE-FOUNDATION.md](./domains/57-PAYMENT-VERIFIED-CAPTURE-FOUNDATION.md). Validation: build/lint/format ✓, **340/340 tests** (323 prior + 17 new: signature/amount/identity/capture/webhook/transaction/concurrency), `prisma validate` ✓, no migration. **NO refund/wallet/settlement/coupon/order-lifecycle/schema change.** Recommends **P1.7.29 = Refund + wallet-credit foundation** (resolves OD-REF-1 reversal on full refund).

- **P1.7.29 — Refund + Wallet-Credit Foundation: COMPLETE** (one **additive** migration `20260903000000_p1_7_29_refund_idempotency`). `RefundService` + `RefundRepository` in `apps/api/src/modules/payment/`: a **WALLET** refund (full or partial) against a **CAPTURED** `PaymentIntent`, performed in one `prisma.$transaction` as `Refund` (`method=WALLET`, `status=PROCESSED`) + `WalletEntry` (`direction=CREDIT`, `balanceAfterMinor` running ledger, `refType=REFUND`) + one `Transaction` (`type=REFUND`, `direction=CREDIT`, linked `walletEntryId`). Refunds are **wallet-only + synchronous**, matching the reconciled legacy ordering behaviour (doc 56 §16). **Remaining refundable = captured − Σ(PROCESSED refunds)**; rejects `≤0` / `> remaining`; enforced under a per-`PaymentIntent` `SELECT … FOR UPDATE` lock (+ per-`Wallet` lock for the balance read-modify-write) so **total refunds ≤ captured** under concurrency (intent-then-wallet lock order; no distributed locking). Wallet owner = the order's customer (`Wallet.userId @unique`); a guest order (no `userId`) is rejected. **Full refund** (cumulative == captured) sets `PaymentIntent → REFUNDED` and reverses the order's `ACTIVE` `CouponRedemption → REVERSED`+`reversedAt` (**OD-REF-1**) — reusing the P1.7.25 lifecycle, predicated on `status=ACTIVE` (idempotent), creating/consuming no coupon and **not moving the order-placement commit point**; **partial refund** sets `PARTIALLY_REFUNDED` and leaves the coupon `ACTIVE` (legacy reverses only on full cancellation). **Idempotent** via the new `Refund.idempotencyKey @unique` (pre-check + unique-constraint rollback → `created=false`; no duplicate wallet/txn/coupon effects); a rejected/failed refund creates **no** financial effect. Canonical target enums only (no legacy `t_type`/`transaction_type`; no historical mapping). **Refund authorization / HTTP boundary + live Razorpay refund (async `INITIATED→PROCESSED`, provider refund id) are deferred** (service-only foundation; no unauthenticated money-moving endpoint). Schema: additive `Refund.idempotencyKey @unique` + `@@index([paymentIntentId])`. Docs: [domains/58-REFUND-WALLET-CREDIT-FOUNDATION.md](./domains/58-REFUND-WALLET-CREDIT-FOUNDATION.md). Validation: build/lint/format ✓, **351/351 tests** (340 prior + 11 new: full/partial/multi-refund, remaining-amount, idempotency, concurrency, coupon reversal, failure paths), `prisma validate` ✓, `migrate status` up to date (12 migrations; applied dev+test). **NO settlement/payout, NO historical migration, NO coupon/payment commit-point change; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.30 — Live Razorpay Refund Integration & Authorization: COMPLETE** (one **additive** migration `20260903010000_p1_7_30_refund_provider_id`). Adds the authorized, asynchronous provider-refund boundary on top of P1.7.29. **`RefundService.requestRefund(principal, input)`** enforces merchant tenancy (`MerchantScopeService.assertRestaurantInScope` on the payment's order restaurant; another merchant → `Forbidden`; SUPER_ADMIN platform-wide). **RAZORPAY refunds are async**: reserve (create an `INITIATED` `Refund` that reserves the amount; **no** wallet credit yet) → `RazorpayRefundGateway.requestRefund` (isolated provider boundary; production `POST /v1/payments/:id/refund` — live HTTP **stubbed pending prod credentials**, returns deterministic `pending`) → persist `providerRefundId`. The P1.7.29 financial effects (wallet credit + `WalletEntry(CREDIT)` + one `Transaction(REFUND/CREDIT)` + `PaymentIntent` advance + full-refund `CouponRedemption` reversal) — refactored into a single shared method — are applied **exactly once** only at the authoritative **`PROCESSED`** point, reached via the `refund.processed` webhook (async) or a synchronous provider `processed`. `refund.failed` → `FAILURE` (releases the reservation). **Schema:** new `Refund.providerRefundId @unique` (one provider refund → one internal Refund; webhook keyed on it). **Idempotency/concurrency (all DB-enforced):** `idempotencyKey @unique` (repeat request = existing refund, **no** 2nd provider call), `providerRefundId @unique`, `WebhookEvent.providerEventId @unique`, per-`PaymentIntent` `SELECT … FOR UPDATE` lock + `INITIATED→PROCESSED` compare-and-set ⇒ **total refunds ≤ captured** and **one provider refund = one wallet entry = one transaction = ≤ one coupon reversal**; `remaining = captured − Σ(INITIATED+PROCESSED)` reserves pending refunds. **Recovery:** a gateway timeout leaves the refund `INITIATED` (reserved) and a retry with the same key does **not** re-issue a provider refund (documented residual: such a refund lacks a `providerRefundId` → reconciliation). Webhook (P1.7.28) extended for `refund.processed`/`refund.failed`; raw-body HMAC verified; duplicate = no-op. Full vs partial refund and the order-placement commit point are **unchanged** (P1.7.29). Secrets config-only (never logged); no client-asserted completion. Docs: [domains/59-LIVE-RAZORPAY-REFUND-INTEGRATION.md](./domains/59-LIVE-RAZORPAY-REFUND-INTEGRATION.md). Validation: build/lint/format ✓, **361/361 tests** (351 prior + 10 new: authorization, provider lifecycle, webhook processed/failed/duplicate, full/partial, reservation reconciliation, idempotency, concurrency, timeout-recovery, signature), `prisma validate` ✓, `migrate status` up to date (13 migrations; applied dev+test). **OD-REF-1 → RESOLVED** (caveat: live Razorpay HTTP call stubbed pending credentials; boundary + async lifecycle + idempotency complete). **NO settlement/payout, NO historical migration, NO coupon/payment commit-point change; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.31 — Settlement & Payout Foundation: COMPLETE** (one **additive** migration `20260903020000_p1_7_31_settlement_payout`). New `apps/api/src/modules/settlement/` over the EXISTING `Settlement`/`SettlementItem`/`Payout`. **`SettlementService.settleMerchant(principal, input)`** (SUPER_ADMIN-only; merchant staff → `Forbidden`) **derives** the settlement from the P1.7.28–30 ledger — never from UI/client/legacy totals: per captured `PaymentIntent`, `net = amountMinor − Σ(PROCESSED Refund.amountMinor)`; `grossAmountMinor = Σ net (= Σ items)`; `commissionMinor = floor(gross × commissionBps / 10000)` (**exact BigInt**; `commissionBps` is an explicit `[0,10000]` input, **never hardcoded** — the per-merchant rate SOURCE is a deferred owner decision); `amountMinor (net payout) = gross − commission`. **Eligibility:** `CAPTURED`/`PARTIALLY_REFUNDED`, not-yet-settled, merchant-isolated via `Order.merchantId` (+ optional `restaurantId`); fully-refunded (net ≤ 0) excluded; unverified/failed never settle. **Settle-once idempotency:** new `SettlementItem.paymentIntentId @unique` (a payment contributes to ≤ 1 settlement item; a second run finds nothing; concurrent runs collide on the unique → one commits). **Payout** = distinct disbursement layer (accrual ≠ money delivered): `requestPayout` reserves a `PENDING` `Payout` for the net, calls the isolated **`RazorpayxPayoutGateway`** (production RazorpayX Payouts API; **live HTTP deferred / not production-ready**; dev returns deterministic `pending`), persists `providerPayoutId`; lifecycle `PENDING → COMPLETED | FAILED` via provider callback (`markPayoutProcessed`/`markPayoutFailed`, idempotent compare-and-set) — `COMPLETED` advances the settlement; a successful settlement never auto-completes the payout. **Payout idempotency:** new `Payout.idempotencyKey @unique` + `providerPayoutId @unique` (repeat request = one provider payout; duplicate callback = no-op; one payout instruction = ≤ one provider payout). **Zero-net settlement** can't be paid out (no negative payout). **Post-settlement refund** processes at the payment layer but does NOT retroactively adjust the settlement (negative-adjustment/clawback **deferred**, documented). Auditable: `Σ items = gross`, `net = gross − commission`, each item → `PaymentIntent`/`Order`. **Schema (additive):** `Settlement` += gross/commission/commissionBps; `SettlementItem` += `paymentIntentId` (FK, unique); `Payout` += `idempotencyKey @unique`; `PaymentIntent` += `settlementItems` back-relation. **`settleAfter` deferred-window scheduling** NOT invented (on-demand run; policy documented as deferred). Docs: [domains/60-SETTLEMENT-PAYOUT-FOUNDATION.md](./domains/60-SETTLEMENT-PAYOUT-FOUNDATION.md). Validation: build/lint/format ✓, **378/378 tests** (361 prior + 17 new: eligibility, refund adjustment, commission, idempotency, ownership, authorization, payout lifecycle/failure/idempotency/duplicate-callback/zero-amount, post-settlement refund, concurrency), `prisma validate` ✓, `migrate status` up to date (14 migrations; applied dev+test). **NO historical migration; NO Super-Admin/Merchant/User/Delivery app migration; NO coupon commit-point change; P1.7.28–30 behavior unchanged; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.32 — Commission Config + `settleAfter` Scheduling Foundation: COMPLETE** (one **additive** migration `20260903030000_p1_7_32_restaurant_commission_bps`). Removes the two P1.7.31 limitations (caller-supplied commission; no settlement window). **Commission source (VERIFIED restaurant-scoped):** legacy stored the rate as `restaurant.comissionCode → SubCategory.description` (a % string) looked up **live** at settlement (mutable, no snapshot — technical debt) with basis `Σ(base_amount) − vendor discount` **+18% GST**; the target normalizes it to the authoritative new **`Restaurant.commissionBps`** (integer basis points), which `SettlementService.settleMerchant` **resolves internally** — **callers can no longer supply a rate** (`SettleMerchantInput` dropped `commissionBps`; `restaurantId` is now **required** so the rate is unambiguously restaurant-scoped), validated `[0,10000]`, `null⇒0`, and **snapshotted** onto each `Settlement` (`commissionBps`/`commissionMinor`) so historical settlements are stable if the config later changes. **GST-on-commission (DR-03a) + the precise commissionable basis (DR-COMM-BASIS)** are DEFERRED (not hardcoded); this slice keeps the P1.7.31 net-of-refund basis. **`settleAfter` (VERIFIED window):** legacy `moment().endOf('day').add(2,'days')` IST set at order creation → the target computes it server-side via the pure `computeSettleAfter(capturedAt, N)` = **23:59:59.999 IST of (capture-day + `SETTLEMENT_DELAY_DAYS`[=2])** (fixed +05:30, no DST, no business-day/holiday logic — none in legacy), **anchored on payment capture** (an authoritative event, not legacy's pre-payment order-creation debt); eligibility = `now >= settleAfter`, **no client date/override**, premature payments deterministically excluded, **no scheduler/cron** (on-demand SUPER_ADMIN run, matching legacy's admin trigger). **Post-settlement refund → determination B (deferred):** legacy has **no** clawback/negative adjustment (VERIFIED); the target preserves P1.7.31's no-retroactive-change behavior and documents the future negative-`SettlementItem` model as **DR-COMM-CLAWBACK** (no speculative debt system built). **All P1.7.31 invariants preserved** (settle-once `SettlementItem.paymentIntentId @unique`, concurrency, merchant isolation, refund deduction, zero/negative-net non-payable, payout=net, itemization; RazorpayX live HTTP still deferred). **Schema (additive):** `Restaurant.commissionBps Int?` only (`settleAfter` needs no field — computed from the capture timestamp). Docs: [domains/61-COMMISSION-SETTLEMENT-TIMING-FOUNDATION.md](./domains/61-COMMISSION-SETTLEMENT-TIMING-FOUNDATION.md). Validation: build/lint/format ✓, **385/385 tests** (378 prior − 17 old settlement + 24 rewritten/new incl. IST-boundary pure tests, timing gate, commission-from-config + snapshot stability, ownership, payout lifecycle, post-settlement refund), `prisma validate` ✓, `migrate status` up to date (15 migrations; applied dev+test). **NO historical migration; NO Super-Admin/Merchant/User/Delivery/UI migration; NO coupon commit-point change; P1.7.28–31 behavior preserved; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.33 — Order-Completion Settlement-Eligibility Gate: COMPLETE** (**no schema change; no migration**). Adds the VERIFIED legacy rule (`settlement.class.ts:552-567`) that a payment is settleable only when its **order is COMPLETED**: `SettlementRepository.findEligibleContributions` now also filters `order.status = 'COMPLETED'` (target `OrderStatus`, P1.7.12 lifecycle), composing with the P1.7.32 capture + `settleAfter` gates and all P1.7.31 invariants — a captured, past-window payment on a non-completed order is excluded. Query-filter change only. The legacy **cancelled + Dunzo-`delivery_partner=2` + `settlementReady`** partial-settlement branch is **DEFERRED** (delivery-domain, out of baseline; `delivery_partner`/`settlementReady` not modeled — DR-SETTLE-CANCELLED-PARTIAL; no delivery fields invented). Docs: [domains/62-ORDER-COMPLETION-SETTLEMENT-GATE.md](./domains/62-ORDER-COMPLETION-SETTLEMENT-GATE.md). Validation: build/lint/format ✓, **387/387 tests** (385 prior + 2; the settle-eligible test helper now advances orders to COMPLETED via the P1.7.12 status lifecycle), `prisma validate` ✓, `migrate status` up to date (15 migrations; no new migration). **NO historical migration; NO Super-Admin/Merchant/User/Delivery/UI migration; P1.7.28–32 behavior preserved; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.34 — Commission Basis + GST/Economic Reconciliation: COMPLETE** (reconciliation + bounded implementation; one **additive** migration `20260903040000_p1_7_34_settlement_commission_basis`). Resolves **DR-COMM-BASIS**. **VERIFIED legacy commission** (`settlement.class.ts:45-52` `totalComission`): `((Σ base_amount − Σ vendor discount) × rate%) × 1.18`, where `base_amount` = pre-discount item subtotal (`usercart.class.ts:122-127`) **excluding** tax/delivery/tips/fees; ADMIN-funded discount is **not** subtracted (commission on full base); basis is **frozen/refund-independent**; **no commission clawback** after settlement. **Target correction:** commission is now charged on the **commissionable basis** — `commissionBasisMinor = Σ(Order.subtotalMinor − (offer.settlementType === 'ADMIN' ? 0 : discountTotalMinor))` (clamped ≥ 0), computed from existing P1.7.12/22/24 fields (no blocked dependency) — instead of the prior tax/delivery-inclusive net-of-refund captured amount; `grossAmountMinor` remains the net-of-refund **payout pool**; `commissionMinor = floor(basis × commissionBps / 10000)`; `amountMinor = gross − commission`. Refunds reduce the payout pool only (basis frozen), matching legacy. New `Settlement.commissionBasisMinor` persists the basis for audit. **GST-on-commission** (legacy hardcoded 18%, bundled into the payout deduction, jurisdiction-specific) = **DR-03a — BLOCKED — OWNER/DATA** — not implemented, not guessed. **DEFERRED:** ADMIN-discount payout reimbursement (legacy `settleAmount += discount`), payout-pool gateway/tip/donation charges, post-settlement clawback (**DR-COMM-CLAWBACK**), cancelled/Dunzo partial (DR-SETTLE-CANCELLED-PARTIAL). Reconciliation invariants tested. Commission config source (`Restaurant.commissionBps`, snapshotted) unchanged (P1.7.32). Docs: [domains/63-COMMISSION-BASIS-GST-RECONCILIATION.md](./domains/63-COMMISSION-BASIS-GST-RECONCILIATION.md). Validation: build/lint/format ✓, **391/391 tests** (387 prior + 4: subtotal-only basis excluding tax/delivery, VENDOR-discount subtracted, ADMIN-discount not subtracted, basis frozen under partial refund), `prisma validate` ✓, `migrate status` up to date (16 migrations; applied dev+test). **NO historical migration; NO Super-Admin/Merchant/User/Delivery/UI migration; P1.7.28–33 behavior preserved; DR-02b/c/d/e remain BLOCKED.**

- **P1.7.35 — Payout Charges & Financial Deduction Reconciliation: RECONCILIATION COMPLETE** (**forensic only — no code/schema/migration/test change**). Traced the full legacy merchant-payout formula (`settlement.class.ts:733-750`): `orderPayAmount = Σ(settleAmount − tip − donation − calcRazorpayCharges − gstTaxes) − commission`, where `settleAmount = total_amount (+ discount for ADMIN offers)`, `calcRazorpayCharges = (Σ RAZORPAY/WALLET payment − donation) × OUTGOING_CHARGES%(def 3) × 1.18`, and tips are disbursed separately (`PAYOUT_TYPE.ORDER_TIP`). **Outcome — every additional payout deduction/addition beyond the current target model is deferred/blocked:** (1) **tips** & (2) **donations** are **not modeled in the target Order** (no fields) — DR-TIP / DR-DONATION (donations are a charity passthrough to `UDBHAV_ACCOUNT`, not merchant-owned); (3) **customer-GST remittance** (legacy deducts `gstTaxes` from payout) + (4) **GST-on-outgoing-charge** + GST-on-commission = **DR-03a — BLOCKED — OWNER/DATA**; (5) **gateway/outgoing/payout charge** rate (`OUTGOING_CHARGES` subcategory %) is config-not-in-target + carries GST → **DR-PAYOUT-CHARGE — DEFERRED**; (6) **ADMIN-discount reimbursement** (`settleAmount += discount`, `user-ordering.class.ts:3543`) is VERIFIED for happy/partial-refund but has an **unresolved full-refund-on-completed-order clawback edge** → **DR-ADMIN-DISCOUNT-REIMBURSEMENT — DEFERRED**; (7) delivery split = delivery domain. The current target settlement (gross = net-of-refund captured; commission on the P1.7.34 basis; net = gross − commission) is **confirmed correct/minimal**; **no component is conclusively implementable without guessing**, so no code/schema change was made (forensic-first rule). Full component matrix, per-component evidence, refund interaction, financial invariants, and the future target model (separate additive `Settlement` components + Order `tipMinor`/`donationMinor`) are documented. Docs: [domains/64-PAYOUT-CHARGES-DEDUCTIONS-RECONCILIATION.md](./domains/64-PAYOUT-CHARGES-DEDUCTIONS-RECONCILIATION.md). **No implementation** (391/391 unchanged). Recommends **P1.7.36 = Tip & donation Order-model foundation** (the GST-independent components blocked only by missing Order fields) or escalating **DR-03a (GST)**.

- **P1.7.36 — Tip & Donation Order-Model Foundation: COMPLETE** (data-model foundation slice; one **additive** migration `20260903050000_p1_7_36_order_tip_donation`). Adds two canonical customer-funded amounts to `Order` — **`tipMinor`** and **`donationMinor`** (`BigInt`, **NOT NULL default 0**) — the GST-independent components P1.7.35 deferred **only because the fields were missing** (DR-TIP/DR-DONATION), not because of the blocked GST decision. **Forensic-first:** traced the target order-total contract and found the DB `order_total_integrity` CHECK fixes `grand = subtotal − discount + tax + fee + delivery`; therefore the **only deterministic implementation that preserves total semantics** is standalone columns held **OUTSIDE grand-total and OUTSIDE the commission basis** (adding them to `grand` would require changing the CHECK = explicitly out of scope). Legacy evidence: tip is **separately disbursed** (`PAYOUT_TYPE.ORDER_TIP`, `settlement.class.ts:739,741`) and **excluded from commission** (VERIFIED P1.7.34); donation is a **charity pass-through** to `UDBHAV_ACCOUNT` (`razorpay.class.ts:863-921`), **not merchant revenue**, excluded from commission. Wired **service-only** (`CreateOrderInput` optional → `OrderService` validate `>=0` + pass-through → repo persist → `OrderRecord` read); **no REST surface added** (ordering has no controller/DTO); payment capture still uses `grandTotalMinor` (unaffected). **Settlement economics UNCHANGED** — tests assert identical `grossAmountMinor`/`commissionBasisMinor`/`commissionMinor`/`netAmountMinor` with vs without a large tip+donation. **Refund semantics for tip/donation DEFERRED** — captured grand excludes them, so no deterministic refund/apportionment info yet (not invented). Migration **additive-only**: verified all **1681** existing `Order` rows backfilled to `0/0`, no existing column/total/constraint changed, `order_total_integrity` byte-for-byte unchanged, rollback = `DROP COLUMN`. Docs: [domains/65-TIP-DONATION-ORDER-MODEL-FOUNDATION.md](./domains/65-TIP-DONATION-ORDER-MODEL-FOUNDATION.md). Tests: new `order-tip-donation.e2e-spec.ts` (10) + ordering/settlement suites green; **full suite 391/391 → 401/401**.

- **P1.7.37 — Tip/Donation Lifecycle & Economic Contract: RECONCILIATION COMPLETE** (**forensic only — no code/schema/migration/API/DTO change**). Traced `Order.tipMinor`/`Order.donationMinor` end-to-end. **Confirmed current behavior (VERIFIED file:line):** (1) creation is the ONLY write path (`order.service.ts:134-135,180-181` → `order.repository.ts:209-210`); `updateStatusWithEvent` sets only `status` (`:312`) — never mutated post-creation, never null (NOT NULL default 0), never negative (validated `>=0`); (2) **capture excludes them** — `createIntent` amount = `order.grandTotalMinor` (`payment.service.ts:43`), `verifyAndCapture`/webhook require `= intent.amountMinor` (`payment.service.ts:79`, `razorpay-webhook.service.ts:111`); (3) **refund ceiling excludes them** — `remaining = intent.amountMinor − reserved` (`refund.repository.ts:318`); (4) **settlement excludes them** — basis/gross read only `subtotal`/`discount`/`intent.amountMinor` (`settlement.repository.ts:86-93`), `payoutType` hardcoded `'ORDER'` (`:130`); (5) **no disbursement/charity code** — `SettlementPayoutType.ORDER_TIP`/`SCAN_AND_PAY` exist (`schema.prisma:131-135`) but are never produced; no tip/donation beneficiary, `UDBHAV`, or transfer. **Critical conclusion:** the whole money chain is anchored on `grandTotalMinor = PaymentIntent.amountMinor`, and P1.7.36 keeps tip/donation OUTSIDE it, so **the customer is never charged for tip/donation — they are recorded intent only, with no collected money, no liability, and no refundability.** The reconcilable gap vs legacy (`settleAmount = total_amount` INCLUDED tip+donation) is the **missing money-in (collection) step**. Documented decision record A–I: data available (order/payment/settlement ledger) vs missing (a collected tip/donation amount, beneficiary identity, charity-liability ledger, tip/donation refund linkage, provider transfer ids); required invariants (collection precedes disbursement; isolation from commission/CHECK; idempotent at-most-once disbursement via existing unique-key/compare-and-set patterns; refund symmetry; exact BigInt/conservation; server-side beneficiary). **Smallest safe next boundary = money-in first.** Docs: [domains/66-TIP-DONATION-LIFECYCLE-ECONOMIC-CONTRACT.md](./domains/66-TIP-DONATION-LIFECYCLE-ECONOMIC-CONTRACT.md). **No implementation** (401/401 unchanged; `tsc`/lint clean). **P1.7.37 Go/No-Go: NO-GO** for tip/donation payout routing, charity transfer, beneficiary resolution, or tip/donation refund next; **GO** for **P1.7.38 — Tip/Donation Collection (Capture) Foundation**, conditional on an owner decision confirming the India baseline collects tips/donations and the collection mechanism. `order_total_integrity`, commission basis, and settlement economics **unchanged**.

## Current activity

- **P1.7.37 complete (forensic reconciliation).** The tip/donation economic lifecycle is fully mapped: they are recorded customer intent only (outside `grandTotalMinor`), so nothing is collected, disbursed, or refundable yet. **Next slice P1.7.38 = Tip/Donation Collection (Capture) Foundation** (model money-in separate from grand-total/commission/CHECK, with idempotency) — **owner-decision gated** on whether/how the India baseline collects tips/donations; only after collection exists can disbursement (tip payout), charity transfer, and tip/donation refund be built. GST stays **DR-03a — BLOCKED**. **401/401. No frontend migrated.** Prior context: **P1.7.36 complete (data-model foundation).** `Order` now records `tipMinor`/`donationMinor` canonically, excluded from grand-total and commission, with settlement economics unchanged and refund semantics deferred. This unblocks a future settlement slice that routes tip (separate disbursement) and donation (charity pass-through) — which would additionally need the payout/capture wiring (DR-TIP/DR-DONATION payout side) and, for the tax-dependent deductions, **DR-03a (GST)**. **401/401. No frontend migrated.** Prior context: **P1.7.35 complete (forensic reconciliation).** The full legacy payout-pool flow is mapped; every deduction beyond the current model (tips, donations, gateway/outgoing charge, customer-GST remittance, ADMIN reimbursement) is deferred/blocked with a named owner decision (tips/donations not in the target Order; GST → DR-03a BLOCKED; outgoing-charge rate config + GST; ADMIN reimbursement full-refund clawback unresolved). The current target settlement/commission model is confirmed correct/minimal. **No code/schema change.** **Recommended next slice P1.7.36 = Tip & donation Order-model foundation** (add additive `Order.tipMinor`/`donationMinor` with passthrough/commission-excluded semantics so a later settlement slice can deduct them) — or escalate **DR-03a (GST)** to unblock the tax-dependent payout economics. **391/391. No frontend migrated.** Prior context: **P1.7.34 complete (reconciliation + implementation). DR-COMM-BASIS RESOLVED.** Commission is now charged on the VERIFIED legacy basis (pre-discount subtotal minus vendor-funded discount; excludes tax/delivery/tips; ADMIN discount not subtracted; refund-independent), persisted as `commissionBasisMinor`; the tax/delivery-inclusive captured amount is no longer the commission base. GST-on-commission stays **DR-03a BLOCKED — OWNER/DATA** (not guessed); ADMIN reimbursement, payout charges, and post-settlement clawback (DR-COMM-CLAWBACK) are documented deferrals. **Recommended next slice P1.7.35 = GST / tax-on-commission foundation (DR-03a)** once the owner provides the India GST rate/components + jurisdiction model (record GST separately on the settlement; localization-ready) — or a payout-charge model slice, or delivery-domain reconciliation. **391/391. No frontend migrated.** Prior context: **P1.7.33 complete (implementation).** Settlement eligibility now requires the order to be COMPLETED (VERIFIED legacy rule) on top of payment capture + `settleAfter`; captured/past-window payments on non-completed orders are excluded. Query-filter only, no schema change. The cancelled-Dunzo partial-settlement branch is deferred to the delivery domain. **Recommended next slice P1.7.34 = DR-COMM-BASIS resolution** (reconcile the exact commissionable basis — legacy commissions on `base_amount`/subtotal minus vendor discount, excluding tax/delivery/tips; target currently commissions on net-of-refund captured — pending the GST/fee model DR-03a), or begin **delivery-domain reconciliation** (which DR-SETTLE-CANCELLED-PARTIAL depends on). **387/387. No frontend migrated.** Prior context: **P1.7.32 complete (implementation).** Settlement now resolves commission from authoritative config (`Restaurant.commissionBps`, snapshotted; no caller override) and gates on a server-derived, capture-anchored `settleAfter` window (end-of-day IST + 2 days); premature payments are excluded; post-settlement clawback + GST-on-commission + commissionable-basis are documented deferrals. One additive migration; no historical migration. **Recommended next slice P1.7.33 = Order-completion settlement-eligibility gate** (legacy also requires `order_status=COMPLETED` to settle; add this condition using the existing `OrderStatus` lifecycle) — or a **DR-COMM-BASIS** resolution slice. **385/385. No frontend migrated.** Prior context: **P1.7.31 complete (implementation).** A merchant settlement + payout foundation exists: SUPER_ADMIN settles a merchant the net-of-refund captured amount minus (explicit) commission — derived from the payment/refund ledger, exact BigInt, settle-once (`SettlementItem.paymentIntentId @unique`), merchant-isolated, auditable (`Σ items = gross`, `net = gross − commission`) — and disburses via a separate idempotent `Payout` through an isolated (deferred-live) RazorpayX gateway; accrual ≠ disbursement; zero/negative payout impossible; post-settlement refund clawback + commission-rate source + `settleAfter` scheduling are documented deferrals. One additive migration; no historical migration; no app-surface migration. **Recommended next slice P1.7.32 = Commission-config + `settleAfter` scheduling foundation** (resolve the commission-rate source + deferred-settlement window; additive schema) — or a **post-settlement refund clawback** slice. **378/378. No frontend migrated.** Prior context: **P1.7.30 complete (implementation). OD-REF-1 RESOLVED.** The live Razorpay refund boundary exists: authorized (merchant-scoped) async refunds — reserve → provider → INITIATED → (webhook/sync) PROCESSED — apply the P1.7.29 financial effects exactly once, with `providerRefundId`-keyed idempotency, per-intent-locked total ≤ captured, and full-refund coupon reversal; timeout recovery issues no duplicate. The only remaining production task is wiring the live Razorpay HTTP call inside the isolated gateway (credentials infra-managed); the reconciled semantics do not change. **Recommended next slice P1.7.31 = Settlement & payout foundation** (merchant `Settlement`/`SettlementItem`/`Payout` net of refunds; commission + `settleAfter` per doc 56 §29; additive schema, no historical migration). **361/361. No frontend migrated.** Prior context: **P1.7.29 complete (implementation).** A wallet-refund foundation exists: full/partial refunds against a captured payment credit the customer `Wallet` via a `WalletEntry` ledger + one `Transaction`, with total-refunds-≤-captured enforced under a per-intent lock, DB-enforced idempotency, and **full-refund coupon reversal (OD-REF-1 advanced)** — no settlement, no commit-point move, no historical migration, one additive migration. **OD-REF-1 stays PARTIALLY RESOLVED** (wallet-refund reversal is authoritative + tested; live Razorpay refund path + refund authorization remain, and the redemption commit point stays order placement). **Recommended next slice P1.7.30 = Settlement & payout foundation** (merchant `Settlement`/`SettlementItem`/`Payout` net of refunds; commission/`settleAfter` fields per doc 56 §29) — or a live Razorpay-refund integration slice to close OD-REF-1. **351/351. No frontend migrated.** Prior context: **P1.7.28 complete (implementation).** A minimal, verifiable payment foundation exists: server-verified Razorpay capture (signature + intent + exact-BigInt amount/currency) creating exactly one `Transaction` per provider payment, plus idempotent `WebhookEvent` ingestion — over the existing schema, no migration, no mutable state, DB-enforced idempotency. Coupon-redemption commit point stays **order placement** (OD-REF-1 unchanged); refund/wallet/settlement and all historical migration remain deferred; DR-02b/c/d/e stay BLOCKED (data-migration only). **Recommended next slice P1.7.29 = Refund + wallet-credit foundation** (model `Refund` + `WalletEntry` ledger; reverse `CouponRedemption` on full refund — resolves OD-REF-1). **340/340. No frontend migrated.** Prior context: **P1.7.27 complete (discovery).** The payment/refund runtime is mapped: legacy Razorpay is client-orchestrated with **no server verification** and a **stub webhook**; ordering refunds are **wallet-only + cancel-gated**; coupon usage is double-counted and cancel-reversed only; numeric enums are env-injected (integers not in source). The target payment schema is present, unused, and stronger than legacy. **DR-02b/c/d/e stay BLOCKED — OWNER/DATA** (need env/DB census); **OD-REF-1 partially resolved** — the coupon-redemption commit point stays at **order placement** until a verifiable payment module exists. **Recommended next slice P1.7.28 = Payment Intent & Verified-Capture Foundation** (signature-verified capture + idempotent webhook ingestion; no refund/wallet/settlement; no commit-point change). **No code/schema/migration/test change; 323/323.** Prior context: **P1.7.26B complete (implementation).** Offer usage-frequency (`useLimit`/`useFrequency`) is now enforced for global offers as a per-user, per-IST-calendar-period cap (Sunday-start weeks), derived from ACTIVE `CouponRedemption` under the existing per-coupon lock — no schema change, no mutable counters, no payment/refund/cancellation change. Cancellation releases a period slot via the P1.7.25 `REVERSED` lifecycle; refund reversal remains deferred (OD-REF-1). **323/323. No frontend migrated.** Prior context: **P1.7.26A complete (discovery). OD-USG-1/2/3 RESOLVED.** The usage-frequency semantics are now fully source-established: per-user, per-`useFrequency` calendar period, anchored to **Asia/Kolkata (IST)**, **Sunday**-start weeks, gated to **global** offers in legacy; representable in the target via derived `CouponRedemption` counts with no schema change and no mutable counters, serialized by the existing per-coupon `FOR UPDATE` lock. Cancellation releases a slot (target `REVERSED`); refund/payment-failure release remains deferred (OD-REF-1). **Recommended next slice P1.7.26B = implement usage-frequency enforcement** (IST + Sunday-start, applied whenever configured, under the existing coupon lock; no payment/refund, no schema change). **No code/schema/migration/test change; 305/305.** Prior context: **P1.7.25 complete (implementation + reconciliation).** Order cancellation now reverses the coupon redemption exactly once and consistently under concurrency (compare-and-set in the same transaction), releasing derived usage; no mutable counters, no schema change. `useLimit`/`useFrequency` were reconciled to a source-clear per-user, per-calendar-period rule but **deferred (CASE B)** because the window timezone anchor + week-start are not source-established (owner decisions). Refund/payment reversal remains deferred until a payment lifecycle exists; **order placement stays the commit point.** **Recommended next slice P1.7.26 = usage-frequency enforcement (after OD-USG-1/2/3) + payment/refund commit-point foundation.** **305/305. No frontend migrated.** Prior context: **P1.7.24 complete (implementation).** Offer redemption + server-side discount now runs at order creation: a coupon is validated server-side, the discount + grand total are computed and persisted authoritatively (client values ignored), and one idempotent, reversible `CouponRedemption` is written in the same transaction with usage derived from ACTIVE rows and enforced under a per-coupon row lock (no oversubscription). Cancellation reverses the redemption. **Payment/refund/settlement/Cart/Experience remain deferred; commit point = order placement.** **Recommended next slice P1.7.25 = order cancellation/refund reversal + per-period usage (`useLimit`/`useFrequency`) foundation.** **293/293. No frontend migrated.** Prior context: **P1.7.23 complete (discovery). DEC-OFF-1 + DEC-OFF-3 resolved.** The commercial runtime is mapped: legacy trusts client discount/total (target will make it server-authoritative) and double-counts usage without a redemption ledger (target will use one idempotent, reversible `CouponRedemption` at the commit point). Experience promotion dormant; seating independent. **Recommended next slice P1.7.24 = offer redemption + server-side discount at order creation** (extend P1.7.12, minimal additive `CouponRedemption` changes; Cart/payment-capture/settlement deferred). **No downstream transactional behavior migrated; no frontend.** Prior context: **P1.7.22 complete.** A merchant/admin can now configure an **Offer + coupon code** (discount type, cap, order gates, validity, active, usage-limit config, service types, settlement type, global/merchant/restaurant scope) in the canonical backend — merchant-scoped, one additive migration, reusing the existing `Offer`(+`Coupon`) with `CouponRedemption` left unused. **268/268.** Redemption, discount calculation/application, order/cart/payment integration, usage counters, SPLIT settlement, multi-restaurant targeting, media, and referral/curation/video systems remain deferred. **No redemption/order/payment/frontend implemented; no ordering/seating/experience behavior changed.** Prior context: **P1.7.21 complete (discovery).** The promotion domain is mapped: Offer=Coupon single legacy entity; discount is client-computed (target should move server-side); adjacent referral/curation/video systems are independent; target Offer/Coupon exist but incomplete; no promotions module. **Recommended next slice P1.7.22 = merchant Offer & Coupon configuration foundation** (config write only; redemption/discount-application deferred to the transactional slice). **No downstream operating domain beyond menu/seating/experience-config migrated; no frontend migrated.** Prior context: **P1.7.20 complete.** A merchant can now define a bookable **Experience** (taxonomy/capacity/pricing/food-mode/menu-mode/publication) + reference its custom menus, in the canonical backend — merchant-scoped, one additive migration. **260/260.** Booking (`expRequest`), payment/refund, Diner/Order, seat inventory, media, packages, events/scraped-events, and customer UI remain deferred. **No booking/payment/event/media implemented; no frontend; no seating/ordering behavior changed.** Prior context: **P1.7.19 complete (discovery).** The Celebrations/Experiences domain (+ Custom Menu + Media Dashboard) is mapped: Experience vs Event vs Festival vs exp-events vs exp_events vs Celebration are distinguished from source; the entire domain is a target gap; Custom Menu reuses P1.7.18; media has no target entity. **Recommended next slice P1.7.20 = merchant Experience configuration foundation** (first-class Experience + CustomMenu reference), deferring booking/payment/events/scraped-events/media-catalogue. **No downstream operating domain beyond menu/seating migrated; no frontend migrated.** Prior context: **P1.7.18 complete.** An activated merchant can now configure a usable Menu and its Items in the canonical backend: `CatalogWriteService` writes Menu/MenuSection/MenuItem/ItemVariant/ItemChannelConfig/AddOnGroup/AddOn over the existing schema (one additive migration), merchant-scoped, with publication distinct from availability and atomic item+children creation. **251/251.** Combos, menu-category tax/charges, scheduled availability, per-variant channel pricing, POS sync, and dietary/media metadata remain deferred owner decisions; no controllers/frontend. **No ordering/seating/offers/experiences behavior changed.** Prior context: **P1.7.17 complete (discovery).** The menu/item WRITE contract is mapped and DEC-3 largely resolved (availability RESOLVED; tax PARTIAL preserve-item-surcharges; combos UNRESOLVED owner decision). **Recommended next slice P1.7.18 = merchant Menu/Item write foundation** over the existing catalog models (+ minimal additive fields), merchant-tenant-scoped, no customer UI / ordering / POS sync / combos. **No downstream operating domain beyond seating migrated; no frontend migrated.** Prior context: **P1.7.16 complete.** The merchant seating configuration + seating-request foundation exists: normalized `SeatingArea`/`RestaurantTable` inventory with a runtime `TableStatus`, and `SeatingRequest` for reservation/walk-in/waitlist — merchant-scoped, over the existing schema (one additive migration). Config gates/timers remain in `Subscription.config`. **242/242.** Deferred runtime pieces (booking→table auto-sync, auto-cancel cron, seats, waiter, QR, experience/event seating, order cross-links) are documented for a later runtime slice. **No downstream operating domain beyond seating migrated; no frontend migrated.** Prior context: **P1.7.15 complete (discovery). DEC-2 RESOLVED (Hybrid).** The Seating domain is understood end-to-end and is ready for a bounded implementation slice (P1.7.16): normalize `table_setup` inventory into `SeatingArea`/`RestaurantTable` (+ a `TableStatus`), keep feature/config gates + timers in `Subscription.config`, and use `SeatingRequest` for walk-in/waitlist/reservation — merchant-scoped (P1.7.1F/P1.7.2), owner-activated (P1.7.14). Table runtime-status + cron parity are SHOULD/deferred sub-decisions; waiter/QR/experience-event seating are FUTURE. **No downstream operating domain migrated; no code/schema changed.** Prior context: **P1.7.14 complete.** A provisioned merchant can now become an authenticated, activated operational merchant: the owner (`StaffMember` MERCHANT_OWNER + credential) is provisioned by SUPER_ADMIN, gated by an activation (owner status) that the existing staff auth enforces, with merchant-scoped restaurant-profile + non-destructive subscription-config update writes. **No schema change; 230/230.** The next candidate configuration slices (evidence-optimized per doc 42): **Subscription-config write depth** (entitlement gates) → **Seating setup** (after DEC-2 `table_setup` decision) → **Menu write** → **Item write** (after DEC-3); Offers/Experiences are parallelizable and don't block Ordering. **No downstream operating domain (seating/menu/items/offers/experiences/ordering) migrated; no frontend migrated.** Prior context: **P1.7.13 complete (discovery).** The configuration dependency chain is mapped and the strategic sequence validated against source. The **recommended next slice is Merchant Owner Provisioning & Onboarding Activation** — it is the only missing HARD prerequisite upstream of all merchant-scoped setup (seating/menu/items/experiences/offers), because a target merchant currently has no `StaffMember` principal and no activation gate. Subsequent evidence-optimized order: Subscription-config write → Seating (after DEC-2) → Menu write → Item write (after DEC-3) → Experiences/Offers (parallelizable) → Cart → Ordering (P1.7.12 done) → Payment → Delivery → Settlement. **No downstream domain migrated; no code/schema changed.** Prior context: **P1.7.12 complete.** Canonical backend **Ordering foundation** exists: merchant-scoped Order + OrderItem creation and the native single-field `OrderStatus` lifecycle with `OrderStatusEvent` history, exact BigInt money, transactional integrity — over the existing schema (no change). Rider 5/6 semantics ride the same `OrderStatus`; payment/delivery/POS/realtime/cart/ONDC remain deferred. **No downstream integration (payment/delivery/POS/realtime) migrated; no frontend migrated.** The next candidate is the **Cart write foundation** (order-origination) or the **Payment/Delivery status-value mapping** slices; Seating (table_setup) and Discovery (Mood/Craving) remain blocked by their owner decisions.

- **P1.7.11 complete (discovery). OD-11 resolved.** The next slice is the **Ordering Foundation** (order create + status lifecycle + items + status events, merchant-scoped, over existing schema; payment/delivery/POS/realtime deferred). **No downstream domain migrated.** Prior context: **P1.7.10 complete.** The canonical create/write path for Merchant → Restaurant → Subscription now exists (platform-provisioned merchant; merchant-scoped restaurant/subscription), unblocking merchant-owned downstream domains structurally. **Overall migration is NOT complete**; downstream domains (Ordering/Seating/Discovery/etc.) remain deferred with their blockers (OD-11, table_setup, Mood/Craving), and the onboarding frontend/wizard + owner-staff provisioning + public signup remain deferred. **No downstream domain migrated.** Prior context: **P1.7.9 complete (discovery).** Onboarding progression is FE-driven; the next slice is the backend Merchant onboarding **write/create** foundation (upstream, unblocked). **No downstream domain migrated.** Prior context: **P1.7.8 complete.** Merchant onboarding-state + user profile-state have canonical, tenant/user-scoped representations over existing models (no new entity, no geography/media/discovery). **Overall migration is NOT complete**; the full onboarding workflow/UI and downstream domains remain deferred. **No downstream business domain migrated.** Prior context: **P1.7.7 complete (discovery).** Onboarding prerequisites are confirmed present; the next slice is the Onboarding/User-Profile foundation over existing models (geography/media/discovery deferred, India-first safe). **No downstream domain migrated.** Prior context: **P1.7.6 complete.** The canonical `Currency` reference exists (supplementing embedded `currencyCode`; monetary representation unchanged). **Overall migration is NOT complete**; geography, media/asset repo, discovery taxonomy, experience/event, FX, payments, and downstream domains remain deferred with documented blockers. **No downstream domain migrated.** Prior context: **P1.7.5 complete.** The merchant-owned Menu & Catalog is readable (tenant-scoped) with exact money + availability/soft-delete semantics, over the pre-existing schema — no ordering/cart/POS/discovery crossed. **Overall migration is NOT complete**; the next downstream domains (Cart → Ordering → Payments/Delivery, Experience food, Discovery, Seating) remain deferred with their documented blockers (OD-11 numeric status, table_setup modeling, missing experience/event schema, discovery-taxonomy UNKNOWN). **No downstream domain migrated.** Prior context: **P1.7.4 complete** — the admin-defined platform taxonomy (`Category`/`Sub Category` hierarchy + `Cuisine`) is import-ready (legacyId), stores embedded icons, and is readable via a foundation module — without building discovery, admin CRUD, or merchant/user UI. **Overall migration is NOT complete**; discovery taxonomy (Mood/Craving), cuisine dedup, taxonomy vocabulary, Experience/Event/Festival taxonomy, and all business domains remain deferred/owner-decisions. **No downstream business domain migrated.** Prior context: **P1.7.3 complete** — Merchant subscription/configuration is safely representable and readable (tenant-scoped) without semantic loss; `config` stays JSON with a safe accessor, and `table_setup` is preserved/documented for the future seating migration. **Overall migration is NOT complete**; subscription-config normalization (incl. table_setup), numeric-status mapping, subscription cardinality, and all business domains remain pending owner decisions/phases. **No business-domain behavior migrated.** Prior context: **P1.7.2 complete** — Merchant (tenant) + Location (restaurant) foundation grounded and usable via read repositories + a data-aware merchant tenancy service (no schema change). **Overall migration is NOT complete**; subscription-config modeling, table-setup, and all business domains (seating/ordering/menu/experiences/events/delivery/…) remain pending, plus the legacy→Merchant import grouping (owner decision). **No business-domain behavior migrated.** Prior context: **P1.7.1F complete** — staff/admin authentication (P1.7.1E) + authorization foundation (P1.7.1F) in place (local/dev, flagged). The reusable guard/decorators are ready for future domain controllers to enforce role/permission/merchant scope. **Overall migration is NOT complete** — the legacy permission catalogue (AUTH-D6), act-as/impersonation, staff registration/OTP/social/MFA/reset, and all business domains remain pending; **O1** and AUTH-D4/D6/D7 remain owner decisions. **No business-domain migration started.** Prior context: **P1.7.1E complete** — staff/admin password authentication works and is validated (local/dev, flagged). **Overall authentication migration is NOT complete** — **RBAC/permission enforcement (P1.7.1F)**, act-as-merchant, staff registration/OTP/social/MFA/reset, and cutover remain pending; **O1** (login-identifier uniqueness) and AUTH-D4/D6/D7 remain owner decisions. **No domain migration started.** Merchant/Location still awaits AUTH-D2 confirmation; OD-11 and owner-decision domains remain blocked.

## Next activity

- **Phase 2 — Restore India baseline** (scope now fixed to the three approved core repos). Order of operations: **restore → validate → freeze → introduce additional capabilities progressively.** Before implementation begins:
  1. Confirm remaining Phase-0 design approvals (B4) and enum mappings (B2); run the secret-rotation workstream (B3).
  2. Decide whether to scaffold the target monorepo skeleton (`apps/`, `packages/`, `prisma/`, `turbo.json`) as the first controlled increment (Decision D-009, pending).
  3. Restore the India baseline in small, reviewable increments, preserving business behavior. Do **not** include the two deferred repositories.

## Blockers

| ID | Blocker | Needed to proceed | Owner |
|----|---------|-------------------|-------|
| ~~B1~~ | **RESOLVED (2026-09-01).** India baseline source confirmed by owner — D-011 APPROVED ([BASELINE_SOURCE_DECISION.md](./india-baseline/BASELINE_SOURCE_DECISION.md)): core = backend + consumer + admin/merchant; the two 2026-era repos deferred. | — | — |
| B2 | **Env-driven enum values** (order/payment status/method, transaction types) are not resolvable from source. | Confirmed integer↔enum mappings before any data/behavior migration. | Backend owners |
| B3 | **Committed secrets** exist in reference env files. | Secret-rotation workstream must run before any baseline restore that touches config. | Security |
| B4 | Target design docs (Phase 0) are **proposals pending review**. | Review/approval of `docs/architecture/*` and open questions in `10-migration-risks.md`. | Architecture review |
| B5 | Unconfirmed: **admin vs merchant** as separate target apps; **ONDC** as a separate bounded context. | Product/architecture decision (see DECISIONS D-006, D-007). | Product + Architecture |

> No blocker is being worked around. Phase 2 does not start until B1 and B4 are resolved.

## Decisions

Recorded in [DECISIONS.md](./DECISIONS.md). Current headline decisions:

| ID | Decision | Status |
|----|----------|--------|
| D-001 | Controlled, incremental replatforming (no big-bang, no wholesale copy) | Accepted |
| D-002 | Reference repositories are read-only; this repo is the only writable target | Accepted |
| D-003 | PostgreSQL as system of record | Accepted (direction) |
| D-004 | Prisma as schema/ORM layer | Accepted (direction) |
| D-005 | India-first; market differences as configuration, not code forks | Accepted (direction) |
| D-006 | Separate `admin` and `merchant` target apps | Proposed |
| D-007 | ONDC as a separate bounded context/service | Proposed |
| D-008 | Monorepo (Turborepo) with NestJS API + Next.js apps | Proposed |
| D-009 | Scaffold monorepo skeleton as first Phase-2 increment | Proposed |
| D-011 | India baseline source set (core = backend + consumer + admin/merchant; 2026 repos = deferred satellites/feature sources) | **Approved** |

## Source repositories (read-only)

| Logical role | Repository | Stack | Notes |
|--------------|------------|-------|-------|
| Platform backend (India) | `amealio-vendordashboard` (`envisionapp`) | Feathers + MongoDB | System of record today; 171 models, ~419 service mounts |
| Consumer web | `amealio_web_app` | CRA / React 18 | Diner web app |
| Admin console | `amealiodashboardmvp-` | CRA / React 16 | Same repo as merchant |
| Merchant dashboard | `amealiodashboardmvp-` | CRA / React 16 | Portal chosen by hostname |
| Delivery tracking API | `amealio-nestjs-backend` | NestJS + PostgreSQL | **Deferred (D-011)** — not in initial baseline |
| Delivery-boy app | `amealio-self-delivery-app` | Next.js | **Deferred (D-011)** — not in initial baseline |

Approved India baseline (D-011): **core** = `amealio-vendordashboard`, `amealio_web_app`, `amealiodashboardmvp-`; **deferred** = `amealio-nestjs-backend`, `amealio-self-delivery-app`. Detail: [01-reference-inventory.md](./01-reference-inventory.md), [SOURCE_REPOSITORIES.md](./SOURCE_REPOSITORIES.md).

## Target architecture (planned — pending review)

- **Monorepo** (Turborepo): `apps/{api,admin,merchant,web}`, `packages/{ui,design-system,types,validation,auth,config,localization,utils}`, `prisma/`, `docs/`.
- **Backend:** NestJS (`apps/api`) replacing the Feathers monolith.
- **Frontends:** Next.js (consumer / admin / merchant).
- **Datastore:** PostgreSQL (system of record) via Prisma; canonical domain model (not a 1:1 collection translation).
- **Market:** India-first (`IN` only); no US-specific behavior yet.

Detail: [../architecture/target-repository-structure.md](../architecture/target-repository-structure.md) and the other `docs/architecture/` documents. Nothing here is scaffolded yet.

## Migrated capabilities

**None yet.** No capability has been migrated or implemented in the target platform.

| Capability | Source repo | Target module | Status |
|------------|-------------|---------------|--------|
| Identity / Auth | `amealio-vendordashboard` | `apps/api/modules/auth`,`users` | Not started |
| Merchant / RBAC | `amealio-vendordashboard` | `apps/api/modules/merchants` | Not started |
| Location / Restaurants | `amealio-vendordashboard` | `apps/api/modules/locations` | Not started |
| Catalog / Menu | `amealio-vendordashboard` | `apps/api/modules/catalog`,`menus` | Not started |
| Order | `amealio-vendordashboard` | `apps/api/modules/orders` | Not started |
| Payment / Settlement | `amealio-vendordashboard` | `apps/api/modules/payments` | Not started |
| Delivery / Tracking | `amealio-vendordashboard`, `amealio-nestjs-backend` | `apps/api/modules/delivery` | Not started |
| Seating / Reservation | `amealio-vendordashboard` | `apps/api/modules/reservations` | Not started |
| Celebration (Experiences/Events) | `amealio-vendordashboard` | `apps/api/modules/celebrations` | Not started |
| Promotion | `amealio-vendordashboard` | `apps/api/modules/promotions` | Not started |
| Notification | `amealio-vendordashboard` | `apps/api/modules/notifications` | Not started |
| Administration / Reporting | `amealio-vendordashboard`, `amealiodashboardmvp-` | `apps/api/modules/admin` | Not started |
| Consumer web UI | `amealio_web_app` | `apps/web` | Not started |
| Admin UI | `amealiodashboardmvp-` | `apps/admin` | Not started |
| Merchant UI | `amealiodashboardmvp-` | `apps/merchant` | Not started |
| Delivery-boy UI | `amealio-self-delivery-app` | (TBD) | Not started |
| ONDC | `amealio-vendordashboard` | separate bounded context | Not started |

Recommended migration order: [10-migration-risks.md](./10-migration-risks.md#4-recommended-migration-order).
